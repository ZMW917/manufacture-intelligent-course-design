# 本次修改文件清单

> 生成时间：2026-09-19
> 来源项目：`f:\ZMW`
> 说明：本文件夹按**原项目的相对路径**存放本次修改/新增的全部文件，可直接覆盖到项目或提交仓库中。
> 共 216 个文件，合计约 44.7 MB（其中 `models/cnn_model.pth` 占 42.7 MB）。

## 一、为什么会有这次修改

本次自查中发现并修复了 **3 个真实缺陷**，它们改变了数据集、模型与全部指标，因此相关文件都需要重新上传：

| # | 缺陷 | 影响面 |
|---|---|---|
| 1 | 气孔类与致密类图像在像素层面不可分（生成器只对裂纹类改形态）→ 该类 CNN 召回率 **0%** | 数据集全部重新生成 |
| 2 | 训练用原图、接口却喂预处理后的 ROI（train-serve skew）→ 线上准确率仅 **37.5%** | 算法代码 + 模型重训 |
| 3 | ROI 被直接拉伸成正方形，抹平长宽比 → 裂纹类召回率 **0%** | 算法代码 + 模型重训 |

修复后：全量准确率 **0.9900**（致密 1.000 / 气孔 0.949 / 裂纹 1.000），pytest **9 项全通过**。

---

## 二、文件清单（按类型）

### A. 代码（4 个，共 4 处修改）

| 文件 | 修改内容 |
|---|---|
| `scripts/generate_sample_data.py` | 气孔类（label 1）在熔池内叠加 3–6 个暗色圆形孔洞，与致密类形成可学差异；图像合成改用独立随机流 `IMG_RNG`（种子 2024），避免今后调整图像形态时连带改变工艺参数序列 |
| `src/algorithms/cnn_classifier.py` | ① `_MeltPoolDataset` 读原图后先走 `preprocess_image` 取 ROI；② `predict()` 改为接收原图、内部完成预处理；③ `_gray_to_tensor` 由直接 resize 改为**等比缩放+居中补零**，保留长宽比 |
| `src/main.py` | `/api/classify` 不再把预处理结果传给分类器（改为传原图），`preprocess_image` 仅用于生成前端回显的 ROI |
| `tests/test_algorithms.py` | 新增用例 `test_cnn_predict_matches_training_pipeline`，断言三类各 2 个样本预测正确，守护"训练与推理走同一预处理链路"这一契约 |

### B. 数据集（204 个文件，全部重新生成）

| 文件 | 变化 |
|---|---|
| `data/raw/images/sample_000.png` ~ `sample_199.png` | 200 张全部重新生成 |
| `data/raw/process_params.csv` | 重新生成。标签分布由 151/38/11 变为 **151/39/10**；能量密度范围由 32.16–133.07 变为 **27.70–185.32** |
| `data/raw/sensor_timeseries.csv` | 重新生成（随机流变化所致） |
| `data/processed/features.csv` | 重新生成。由 200×19 变为 **197×19**（3σ 清洗剔除 3 条） |
| `data/processed/sensor_features.csv` | 重新生成。3σ 标记异常任务由 2 个变为 **3 个**（job 0、2、3，其中 job 2 为统计误报） |

### C. 模型产物（2 个，需重新训练）

| 文件 | 说明 |
|---|---|
| `models/cnn_model.pth` | 用新数据、新预处理链路重训（8 轮，验证集最优权重为第 6 轮） |
| `models/regressor.joblib` | 用新数据重训（RF + SVR） |

### D. 文档（7 个）

| 文件 | 变化 |
|---|---|
| `设计说明书.md` | **新增**。7 章：背景调研 / 方案设计 / 数据集构建 / 4 个技术方向详细设计 / 界面设计 / 调试（5 个缺陷定位过程）/ 总结展望，附 10 条参考文献 |
| `答辩PPT大纲.md` | **新增**。12 页逐页大纲 + 3 分钟时长分配 + 7 分钟问答准备（含 5 段最可能被指定讲解的代码段解析、9 个高概率问题应答要点）+ 演示视频分镜建议 |
| `README.md` | ① 数据说明章节改为"自建数据集（HF 链接）+ 公开基准数据集"；② 目录树补入 `设计说明书.md` 与 `一键启动.bat`；③ 进度补入数据集开源与说明书 |
| `data/README.md` | 重写第二节，加入 Hugging Face 开源链接、数据性质声明、标签分布、引用格式；全部规模数字同步更新 |
| `方案设计.md` | 3.3 节数据集来源重排为"自建数据集（HF 链接）/ 公开基准数据集 / 文件构成"三部分 |
| `选题说明.md` | 五、数据来源章节重写为"自建数据集（HF 链接 + 性质声明）/ 公开基准数据集"两部分，补入此前缺失的开源链接 |
| `选题汇总.md` | ⚠️ **内容未改动**，为**恢复文件**。见下方说明，若你的提交仓库里已有此文件则**无需覆盖** |

### ⚠️ 关于 `选题汇总.md` 的说明

工作过程中发现该文件与任务书 PDF 从项目目录中消失了（**原因不明，非本次操作删除**）。
`选题汇总.md` 已从提交包 `制造智能技术课程设计_智造24-1-28_邹明吾.zip` 中完整恢复（12526 字节，内容与原件一致），
一并放入本文件夹以便你还原到项目中。

任务书 PDF（`制造智能技术课程设计任务书.pdf`）**不在**提交包内（当初打包时按"教师下发件，非学生交付物"排除），
无法从本地恢复，需要向老师重新索取。其全文文本与 3 页渲染图仍保留在 `f:\tmp\` 下（`taskbook_text.txt`、`taskbook_page1~3.png`）。

### E. 运行时产物（1 个，通常不必上传）

| 文件 | 说明 |
|---|---|
| `app.db` | SQLite 运行时数据库，含冒烟测试产生的记录。**程序启动时会自动重建**，原提交包中即已排除。如无特殊需要可不覆盖上传。 |

---

## 三、Hugging Face 数据集

自建数据集已同步更新至：

**<https://huggingface.co/datasets/zoumingwu/lpbf-melt-pool-quality-dataset>**

仓库内 `raw/` 与 `processed/` 的内容与本文件夹中 `data/` 的对应文件一致，dataset card 的统计数字也已同步。

---

## 四、覆盖后的自检步骤

```bash
# 1. 自动化测试（应输出 9 passed）
pytest tests/ -q

# 2. 若模型文件未一并覆盖，重新训练
python -m src.train

# 3. 启动验证
python scripts/run_backend.py
cd frontend && npm run dev
```

覆盖数据与模型后，若只改了代码未重训，会出现训练/推理链路不一致（即缺陷 2），
此时 `pytest` 中的 `test_cnn_predict_matches_training_pipeline` 会失败并给出提示。
