# 答辩 PPT 大纲

**题目**：基于机器视觉与深度学习的激光增材制造（LPBF）熔池在线监测与成形质量智能预测系统
**班级学号姓名**：智造 24-1-28 邹明吾
**答辩结构**：自述 3 分钟 + 问答 7 分钟（教师随机指定 1–2 处代码段现场讲解）

---

## 一、总体设计

### 时长分配（3 分钟 = 180 秒）

| 段 | 幻灯片 | 建议用时 |
|---|---|---|
| 开场 | 1 封面 | 5 s |
| 为什么做 | 2 背景与意义、3 选题定位 | 40 s |
| 做了什么 | 4 系统架构、5 数据集、6 算法、7 界面 | 75 s |
| 做得怎么样 | 8 调试与缺陷修复、9 测试结果 | 45 s |
| 收尾 | 10 AI 使用披露、11 总结展望 | 25 s |

12 页撑满 3 分钟比较赶，**若需压缩，可合并第 6、7 页**（算法与界面并成一页）。
演示视频建议单独录制完整走一遍业务闭环，PPT 只放关键截图。

### 设计原则

- **每页一个观点**，不要把设计说明书的内容直接搬上来
- **数字要能在页面上看到**（0.99、0.375→0.99、9 passed 这类）
- **第 8 页是全场的重心**——"发现并修复了 3 个真实缺陷"比"系统能跑"有说服力得多，时间要给够

---

## 二、逐页大纲

### 第 1 页 · 封面

- 题目全称
- 班级学号姓名：智造 24-1-28 邹明吾
- 开发方式：Vibe Coding（Harness：Claude Code；模型：DeepSeek-v4）
- 一句话定位：**面向 LPBF 金属 3D 打印过程质量控制的 B/S 智能应用**

> 提示：定位那句要在开场 5 秒内说完，让评委立刻知道你做的是什么场景。

---

### 第 2 页 · 背景与研究意义

**页面内容**

- LPBF 是金属增材制造主流工艺，靠激光逐层熔化粉末成形
- 质量对工艺窗口极敏感，两种典型缺陷：
  - 能量密度偏低 → **未熔合 / 裂纹**
  - 能量密度偏高 → **气孔**
- 关键公式：`E = P / (v · t · h)`（J/mm³）
- 痛点：**缺陷产生于成形过程中，成形后很难无损检出，往往整件报废**

**口述要点**

> "LPBF 的成形质量由激光功率、扫描速度、层厚、扫描间距四个参数共同决定，它们合成一个体能量密度。能量偏低粉末熔不透，留下未熔合和裂纹；能量偏高熔池过热卷入气体，形成气孔。问题在于这两类缺陷都是成形过程中产生的，等零件做完再检测就已经晚了。所以要靠熔池在线监测，在过程中就把问题发现出来。"

---

### 第 3 页 · 选题定位与技术方向覆盖

**页面内容**（技术方向映射表）

| 课程技术方向 | 本项目方法 | 系统中的作用 |
|---|---|---|
| 计算机视觉 | OpenCV 预处理、Otsu ROI 提取、直方图增强 | 为 CNN 提供标准化输入 |
| 深度学习 | ResNet-18 三分类（迁移学习） | 图像判定质量等级 |
| 机器学习 | 随机森林 + SVR 回归 | 工艺参数预测致密度 |
| 数据挖掘 | 孤立森林 + SPC（3σ） | 传感器时序异常预警 |

- 补充说明：**机器视觉不是装饰性引入**——CNN 训练与推理都经过同一条预处理链路，摘掉它准确率从 0.99 崩到 0.05（埋个伏笔，第 8 页展开）

**口述要点**

> "任务书要求覆盖至少 3 个课程技术方向。我覆盖了 4 个，而且每个都在系统里承担实际决策职责：视觉做输入标准化，深度学习做图像分类，机器学习做参数回归，数据挖掘做时序异常检测。这四块不是拼在一起的，它们共同支撑了同一条质量控制链路。"

---

### 第 4 页 · 系统架构

**页面内容**（架构图）

```
浏览器（Vue 3 + Element Plus + ECharts）
        │  HTTP / JSON
        ▼
FastAPI 服务层（7 个 REST 接口，自带 Swagger 文档）
   ├── /api/classify  → 图像预处理 + CNN
   ├── /api/predict   → RF / SVR 回归
   ├── /api/anomaly   → 孤立森林 + SPC
   └── /api/history/* → 历史查询
        ▼
SQLite 三张表：process_records / prediction_history / image_records
```

- 规模：后端与算法 1298 行、前端 851 行、测试与脚本 741 行
- 业务闭环：**图像上传 → 熔池监测 → 质量预测 → 异常预警 → 历史留痕**

**口述要点**

> "系统是 B/S 四层架构：前端四个页面走完整业务流程，FastAPI 提供 7 个接口并自带接口文档，SQLite 三张表做记录留痕，下面是三个算法模块。整个链路可以在本地离线运行，不依赖任何在线服务。"

---

### 第 5 页 · 数据集构建与开源

**页面内容**

- **自建数据集**：程序合成的 LPBF 熔池监测数据
  - 200 张熔池灰度图（64×64）
  - 200 条工艺参数 + 质量标签（致密 151 / 气孔 39 / 裂纹未熔合 10）
  - 6 个打印任务 × 100 时间步传感器时序（job 0、3 注入异常尖峰）
- 三类图像的形态设计（**按缺陷物理成因**）：
  - 致密 = 规整椭圆亮斑
  - 气孔 = 亮斑内挖出 3–6 个暗色圆形孔洞
  - 裂纹/未熔合 = 细长亮斑（长宽比约 2.4）
- 已开源至 Hugging Face（CC BY 4.0）：
  `huggingface.co/datasets/zoumingwu/lpbf-melt-pool-quality-dataset`
- 明确声明：**合成数据，非真实企业/实验数据**

**口述要点**

> "数据是合成的，但生成规则按物理成因来设计：标签由能量密度分区决定，图像形态对应缺陷机理——气孔就是熔池里挖出的球状孔洞，裂纹就是细长的未熔合熔池。数据集已经开源到 Hugging Face，用 CC BY 4.0 协议。这里要说明，它是合成数据不是真实产线数据，我在数据卡里也写明了这一点。"

---

### 第 6 页 · 算法设计

**页面内容**（三列并排，每列一个方向）

| 机器视觉 + 深度学习 | 机器学习 | 数据挖掘 |
|---|---|---|
| 灰度化 → 高斯去噪 → Otsu 阈值分割 → ROI 提取 → 直方图均衡 | 5 维特征（4 参数 + 派生能量密度）→ Z-score → RF / SVR | 滑窗统计特征 → 孤立森林；并联 3σ 规则；取并集 |
| ResNet-18：conv1 改 1 通道、fc 改 3 类、离线随机初始化 | RF 特征重要性：能量密度占 **0.7182**，与工艺物理一致 | 连续异常点合并为区间 → 按点数分三档预警 |

**口述要点**

> "三个算法模块各有设计要点。CNN 用的是 ResNet-18，改成了单通道输入、三分类输出，而且故意不加载预训练权重，保证现场断网也能跑。回归这边，随机森林给出的特征重要性里体能量密度占了 0.72，说明模型确实抓住了工艺物理，不是瞎拟合。异常检测用的是孤立森林和 3σ 双判据并联，最后按异常点数分三档预警。"

---

### 第 7 页 · 系统界面

**页面内容**（4 张截图拼版）

| 页面 | 关键交互 |
|---|---|
| 熔池监测 | 拖拽上传 → 原图与 ROI 并排回显 → 三分类概率分布 |
| 质量预测 | 表单录入参数 → 能量密度 → RF/SVR 对比 + 特征重要性 |
| 异常预警 | 选择任务 → ECharts 双轴时序 → 3σ 控制限 + 异常区间高亮 |
| 历史记录 | 预测/图像记录分页表格 |

> 截图建议用真实运行界面，不要用设计稿。

**口述要点**

> "四个页面覆盖完整闭环。熔池监测页会把系统实际提取的熔池区域回显出来，用户能判断 ROI 有没有框错；异常预警页把 3σ 控制限和异常区间直接画在时序图上，异常点为什么被判异常一目了然。"

---

### 第 8 页 · 集成调试：发现并修复的 3 个缺陷 ★核心页

**页面内容**

| # | 缺陷 | 现象 | 根因 | 修复效果 |
|---|---|---|---|---|
| 1 | 气孔类图像不可分 | 该类召回率 **0%** | 生成器只对裂纹类改形态，致密与气孔用了同一组参数（t 检验 p=0.218） | **0% → 100%** |
| 2 | 训练/推理链路不一致 | 离线 0.9900，**线上仅 0.3750**（低于 75.5% 基线） | 训练用原图，接口却喂预处理后的 ROI | **0.3750 → 0.9900** |
| 3 | ROI 被拉伸抹平长宽比 | 裂纹类召回率 **0%** | `cv2.resize` 把细长 ROI 硬拉成正方形 | **0% → 100%** |

- 一句话总结：**状态码正常不等于功能正常——原来的接口测试只校验 200，从不校验预测对不对**

**口述要点**（这是最该讲透的一页，建议 35–40 秒）

> "开发过程中我实测发现了三个缺陷。第一个最严重：气孔类的召回率是 0，39 个样本一个都没判对。我一开始怀疑是类别不平衡，但如果是那样至少该对几个。于是去统计图像本身的形态，发现致密类和气孔类的长宽比做 t 检验 p 值是 0.218，根本没有差别——回到生成脚本一看，生成器只对裂纹类改了椭圆参数，气孔和致密用的是同一个分布。这类问题靠调模型是解决不了的，我按气孔的物理成因给它加了暗色孔洞，召回率恢复到 100%。
>
> 第二个是修完之后发现的：离线评估 0.99，但接口实际只有 0.375，比全判致密的基线还差。原因是训练用原图，而接口把预处理后的 ROI 喂给了模型。第三个是修第二个时连带暴露的：ROI 被硬拉成正方形，细长熔池的长宽比信息被抹掉了，裂纹类又掉到 0，改成等比缩放加补零才恢复。
>
> 这三个问题让我意识到一件事：原来的接口测试只检查状态码是不是 200，从不检查预测结果对不对，所以问题一直藏着。"

---

### 第 9 页 · 测试与验证结果

**页面内容**

| 项目 | 结果 |
|---|---|
| 自动化测试 | pytest **9 项全部通过**（含新增的链路一致性回归测试） |
| 图像分类 | 全量准确率 **0.9900**；逐类召回 致密 1.0000 / 气孔 0.9487 / 裂纹 1.0000 |
| 回归（5 折交叉验证） | RF **R²=0.4719**；SVR R²=0.1443 |
| 回归理论上界 | 贝叶斯最优 R²=0.5669 → **RF 达到上界的 83%** |
| 异常检测 | 注入异常的 job 0、3 判"告警"，区间 `[45,49]` 与注入位置完全吻合 |
| 端到端冒烟 | 7 个接口全部走通，三个类别各 2 个样本 **6/6 判对** |

**口述要点**

> "测试方面，9 个用例全部通过，其中包括我专门为那个链路不一致问题补的回归测试。分类全量准确率 0.99。回归这里要说明一下，训练脚本单次划分给出的 R² 只有 0.095，看起来模型没用，但我算了一下，单次划分的测试集只有 40 个样本，估计极不稳定——改成 5 折交叉验证后 R² 是 0.4719。同时我算了这个数据的贝叶斯最优上界是 0.5669，所以随机森林已经拿到了 83% 的可解释信号。"

---

### 第 10 页 · AI 使用披露

**页面内容**

- **所用工具**：Harness = Claude Code；编程大模型 = DeepSeek-v4
- **关键 prompt 策略**（对应课程五个要点）：
  - 任务拆解：把项目拆成可独立交付的子任务，一次只做一块
  - 上下文工程：维护全局配置与实现契约，**要求 AI 先读代码再改**
  - 规格驱动：先锁定接口签名（输入输出契约），再并行实现
  - 自动化测试 + 人工审查：每个 AI 生成的模块都配测试兜底
  - 接管策略：出错就回滚、缩小改动、构造最小复现
- **AI 出错案例**（第 8 页三个缺陷即为实例）：
  - 并行实现时契约只约定了各模块输入输出，**没约定模块之间如何衔接** → 导致训练/推理链路不一致
  - 接口测试只断言状态码 → 问题潜伏到自查阶段才暴露

**口述要点**

> "开发全程使用 Claude Code 作为 Harness、DeepSeek-v4 作为编程模型，这些在报告里都有披露。这里我想讲一个真实的踩坑：并行开发时我锁定了接口契约，把每个模块的输入输出都写清楚了，但**契约里没写模块之间怎么衔接**，结果预处理和分类各自都没错，组合起来就是错的。这说明光定义接口不够，还要定义调用关系。"

---

### 第 11 页 · 总结与展望

**页面内容**

- **已完成**：B/S 全栈系统 + 4 个技术方向 + 自建数据集开源 + 9 项测试通过
- **不足**（主动讲，别等评委问）：
  - 模型**未在真实数据上验证**，不具备产线迁移能力
  - 图像形态简化，未覆盖飞溅、羽流、匙孔
  - 类别不平衡（裂纹类仅 10 个样本）
  - 无数据漂移监测
- **展望**：接入 NIST AM-Bench 真实数据、类别加权/过采样、SVR 调参、引入 LSTM 时序模型

**口述要点**

> "最后说不足。最大的一个是我所有的验证都是在合成数据上做的，这类模型不能直接拿到产线上用。另外图像形态做了简化，没有覆盖真实的飞溅和匙孔现象。后续最该做的是接入 NIST AM-Bench 这类公开真实数据集，重新验证泛化能力。"

---

### 第 12 页 · 致谢 / Q&A

- "以上是我的汇报，请各位老师批评指正。"

---

## 三、问答环节准备（7 分钟）

### 3.1 最可能被指定讲解的代码段

教师在问答环节会随机指定 1–2 处代码让你现场讲解作用与逻辑。以下 5 处是**最可能被挑中的**，建议逐段吃透。

---

#### 代码段 A：`_gray_to_tensor` 的等比缩放补零

**文件**：`src/algorithms/cnn_classifier.py:81-103`

```python
def _gray_to_tensor(gray, input_size):
    """8-bit 单通道灰度图 -> (1, H, W) 的 float32 tensor（已归一化到 [-1,1]）。

    注意采用「等比缩放 + 居中补零」而非直接拉伸：熔池 ROI 经 cv2.resize 硬拉到
    正方形后，长宽比这一形态信息会被抹平，细长的裂纹/未熔合熔池会退化成普通圆斑，
    导致该类被整体误判。补零到正方形可以保留长宽比。
    """
    h, w = gray.shape[:2]
    scale = float(input_size) / max(h, w)
    new_h = max(1, int(round(h * scale)))
    new_w = max(1, int(round(w * scale)))
    resized = cv2.resize(gray, (new_w, new_h), interpolation=cv2.INTER_LINEAR)

    canvas = np.zeros((input_size, input_size), dtype=np.uint8)
    top = (input_size - new_h) // 2
    left = (input_size - new_w) // 2
    canvas[top:top + new_h, left:left + new_w] = resized

    x = canvas.astype(np.float32) / 255.0             # 归一化到 [0,1]
    x = (x - NORM_MEAN) / NORM_STD                    # 均值/方差 0.5/0.5 -> [-1,1]
    return torch.from_numpy(x).unsqueeze(0)           # 增加通道维 -> (1,H,W)
```

**讲解要点**

1. **作用**：把任意尺寸的熔池 ROI 转成网络要求的 64×64 单通道张量
2. **为什么不用 `cv2.resize(gray, (64,64))`**：那会把图像硬拉成正方形，长宽比信息被抹平。裂纹/未熔合类的熔池是细长的（长宽比 2.4），拉伸后与圆形熔池趋同，该类召回率直接掉到 0
3. **怎么做的**：先按长边算缩放比例做等比缩放，再放进一个 64×64 的全零画布居中放置，两侧留黑边，长宽比得以保留
4. **归一化两步**：先 `/255` 映射到 [0,1]，再按均值 0.5、方差 0.5 标准化到 [-1,1]
5. **`unsqueeze(0)`**：增加通道维，把 `(H,W)` 变成 `(1,H,W)`，因为 `conv1` 已改为单通道输入

**可能的追问**："为什么补零不补其他值？" → 零对应黑色背景，与熔池图像的背景一致，不会引入虚假纹理。

---

#### 代码段 B：`AnomalyDetector.detect` 的双判据并集

**文件**：`src/algorithms/anomaly_detector.py:63-83`（`detect()` 内部节选）

```python
        raw = temp.values
        spc_anomaly = np.where(
            (raw > threshold_upper) | (raw < threshold_lower)
        )[0]

        # 2) 滑动窗口特征（原始值 / 均值 / 标准差），供 IsolationForest 使用
        roll_mean = temp.rolling(window=self.window, min_periods=1).mean().values
        roll_std = temp.rolling(window=self.window, min_periods=1).std(ddof=0).values
        roll_std = np.nan_to_num(roll_std, nan=0.0)
        features = np.column_stack([raw, roll_mean, roll_std])

        # 固定较低污染率，避免 'auto' 在纯噪声序列上误标约 10~25% 的点为异常
        iso = IsolationForest(contamination=self.contamination, random_state=42)
        iso_pred = iso.fit_predict(features)          # 1=正常, -1=异常
        iso_anomaly = np.where(iso_pred == -1)[0]

        # 3) 二者并集（去重 + 升序）
        anomaly_indices = sorted(set(iso_anomaly.tolist()) | set(spc_anomaly.tolist()))

        # 4) 连续异常点合并为区间 [[start, end], ...]
        intervals = self._merge_intervals(anomaly_indices)
```

**讲解要点**

1. **为什么要两种方法并联**：3σ 只能发现"绝对值超出控制限"的点；孤立森林基于"异常点更容易被随机切分孤立"的原理，能发现局部波动异常——比如均值正常但波动幅度变大。两者视角不同，取并集提高召回
2. **为什么孤立森林的输入是 3 维**：`[原始值, 5点滑动均值, 5点滑动标准差]`。只用原始值只能看到绝对水平，加上滑窗统计量才能刻画局部形态
3. **`np.nan_to_num`**：`rolling().std()` 在窗口内样本不足时会产生 NaN，必须置 0，否则孤立森林会报错
4. **`contamination=0.01`**：初版用 `'auto'` 让算法自估，结果在纯噪声序列上误标 10–25% 的点，所有任务都告警。改成固定 1% 才恢复正常
5. **`|` 是集合并集**，`_merge_intervals` 再把连续的异常索引合并成闭区间 `[start, end]`，供前端画高亮区域

**可能的追问**："为什么用并集不用交集？" → 质量控制场景下漏报的代价高于误报，取并集偏向"宁可多报"；同时用点数分档（<3 正常、3–4 注意、≥5 告警）来抑制误报影响。

---

#### 代码段 C：`QualityRegressor.train` 的 SVR 目标标准化

**文件**：`src/algorithms/regression.py`

```python
        self.svr_y_mean = float(y_train.mean())
        self.svr_y_std = float(y_train.std(ddof=0)) or 1.0
        y_train_scaled = (y_train - self.svr_y_mean) / self.svr_y_std

        self.rf = RandomForestRegressor(n_estimators=100, random_state=42)
        self.rf.fit(X_train_scaled, y_train)
        self.svr = SVR(kernel="rbf")
        self.svr.fit(X_train_scaled, y_train_scaled)
```

```python
        density_rf = float(np.clip(self.rf.predict(x_scaled)[0], 0.0, 1.0))
        density_svr = float(np.clip(self.svr.predict(x_scaled)[0] * self.svr_y_std + self.svr_y_mean, 0.0, 1.0))
```

**讲解要点**

1. **问题背景**：致密度的标准差只有 0.021，整个取值范围 0.88–1.00 跨度不到 0.12；而 SVR 默认不敏感带 `epsilon=0.1`。SVR 的损失函数对偏差在 `epsilon` 内的样本给零损失——所有样本偏差都小于 0.1，损失恒为 0，优化无从下手，SVR 退化成输出常数
2. **解决办法**：训练前对目标做 Z-score 标准化，把目标拉到单位量级，与 `epsilon` 匹配；预测后再反变换回原始量纲
3. **注意细节**：标准化用的均值方差来自**训练集**，并且随模型一起保存，预测时复用——不能用测试集统计量，否则是数据泄漏
4. **为什么随机森林不需要**：决策树基于特征阈值分裂，对目标量级不敏感，所以没受影响。这也解释了为什么只有 SVR 出问题

**可能的追问**："`or 1.0` 是干什么的？" → 防止标准差为 0 时除零。

---

#### 代码段 D：`preprocess_image` 的 Otsu 分割与 ROI 回退

**文件**：`src/algorithms/image_preprocess.py:73-93`（`preprocess_image()` 内部节选）

```python
    denoised = cv2.GaussianBlur(gray, (5, 5), 0)

    # 3) Otsu 阈值分割，提取熔池（亮斑）前景区域的外接框作为 ROI
    _, binary = cv2.threshold(denoised, 0, 255, cv2.THRESH_BINARY + cv2.THRESH_OTSU)
    ys, xs = np.where(binary > 0)

    if xs.size == 0:
        # 无前景（全黑图），回退全图
        x, y, bw, bh = 0, 0, w, h
        roi = denoised
    else:
        x0, x1 = int(xs.min()), int(xs.max())
        y0, y1 = int(ys.min()), int(ys.max())
        bw, bh = x1 - x0 + 1, y1 - y0 + 1
        if bw < MIN_ROI_SIZE or bh < MIN_ROI_SIZE:
            # ROI 小于 16×16，视为分割不可靠，回退全图
            x, y, bw, bh = 0, 0, w, h
            roi = denoised
        else:
            x, y = x0, y0
            roi = denoised[y:y + bh, x:x + bw]
```

**讲解要点**

1. **Otsu 的作用**：自动确定把熔池亮斑与背景分开的阈值，不需要人工设阈值。原理是遍历所有可能阈值，选使前景与背景类间方差最大的那个
2. **`np.where(binary > 0)`**：取出所有前景像素的坐标，用坐标的最值算出外接矩形
3. **两道安全阀**：
   - 无前景（全黑图）→ 回退整图
   - ROI 小于 16×16 → 判定分割不可靠，回退整图
4. **为什么需要回退**：如果分割失败却硬裁一小块噪声送进网络，会得到毫无意义的分类结果；回退整图至少让模型看到完整信息

**可能的追问**："16 这个阈值怎么定的？" → 熔池亮斑在 64×64 图上的典型外接框是 27×34 左右，16 只是它的一半不到，作为"明显异常"的判据比较宽松，不会误伤正常样本。

---

#### 代码段 E：`_MeltPoolDataset.__getitem__` 的预处理链路

**文件**：`src/algorithms/cnn_classifier.py:115-127`

```python
    def __getitem__(self, idx):
        img = cv2.imread(self.image_paths[idx], cv2.IMREAD_GRAYSCALE)
        if img is None:
            # 读取失败时退化为全黑图，避免训练中断
            roi = np.zeros((self.input_size, self.input_size), dtype=np.uint8)
        else:
            # 与线上推理走同一条链路：先提取熔池 ROI 并增强，再送入网络
            roi = preprocess_image(img)["enhanced"]
        tensor = _gray_to_tensor(roi, self.input_size)
        label = torch.tensor(self.labels[idx], dtype=torch.long)
        return tensor, label
```

**讲解要点**

1. **关键点**：训练时读入原图后，**先调用 `preprocess_image` 取 ROI 增强图**，再送进 `_gray_to_tensor`
2. **为什么必须这样**：线上接口 `/api/classify` 送入网络的也是预处理后的图像。训练和推理必须走同一条链路，否则就是训练/服务不一致——模型学的是原图的特征，线上给的却是裁剪增强后的图，准确率会从 0.99 掉到 0.375
3. **读取失败的处理**：`cv2.imread` 返回 None 时退化为全黑图，保证训练不中断（对应一个训练样本失效总比整个训练崩掉好）

**可能的追问**："每轮都重新预处理不是很慢吗？" → 确实有重复计算，200 张 64×64 的图预处理开销可接受；如果要优化可以在数据集初始化时缓存一次。

---

### 3.2 其他高概率问题

| 问题 | 回答要点 |
|---|---|
| **为什么用 B/S 不用 C/S？** | 浏览器打开即用无需装客户端；FastAPI 自带 Swagger 便于现场展示接口；ECharts 画时序图比桌面 GUI 省事 |
| **为什么选 ResNet-18 而不是更深的网络？** | 数据量只有 200 张，更深的网络容易过拟合；ResNet 的残差连接解决了深层网络退化问题，18 层在这个规模下够用 |
| **为什么不用预训练权重？** | 演示现场可能没网，依赖在线下载会导致系统起不来；另外 ImageNet 是自然图像，与熔池灰度图差异大，迁移收益有限 |
| **类别不平衡怎么没处理？** | 承认这是不足。裂纹类只有 10 个样本，其召回率指标的统计意义有限。可用类别加权损失或过采样缓解，本次未实施 |
| **回归 R² 为什么这么低？** | 分两层回答：①训练脚本单次 80/20 划分只有 40 个测试样本，R² 极不稳定，5 折交叉验证下是 0.4719；②这个数据生成过程存在固有随机性（同一能量密度下标签按概率混合），贝叶斯最优上界是 0.5669，RF 已经拿到 83% |
| **孤立森林的 contamination 为什么定 0.01？** | 初版用 'auto' 自估，在平稳序列上误标 10–25%，导致所有任务都告警。1% 更符合"过程异常是小概率事件"的业务先验 |
| **数据是合成的，结论有效吗？** | 明确回答：**模型不具备产线迁移能力**。合成数据的价值在于验证系统链路与算法正确性，真实泛化能力必须用 NIST AM-Bench 这类真实数据重新验证。这一点已在数据卡和报告里写明 |
| **系统的业务闭环是什么？** | 图像上传 → 熔池监测 → 质量预测 → 异常预警 → 历史留痕。四个环节对应四个前端页面和四个算法模块，数据通过 SQLite 三张表串联 |
| **这个系统相比不做有什么价值？** | 把分散的视觉、参数、时序三类信号整合进同一条质量判定链路，并留下可追溯的记录；单独看任何一个算法都不复杂，价值在于闭环集成 |

### 3.3 风险提示

- **不要讲自己没吃透的细节**。如果被问到实现细节答不上来，坦诚说"这一点我了解得不够深入"比现编要好——任务书明确写了"答辩中如果发现有 AI 生成，而自己不能说清的地方，将大幅影响成绩，甚至不及格"
- **第 8 页的三个缺陷是你最有把握的部分**，因为是你（我们）实测跑出来的真实数据。但这三个缺陷的定位过程你要能自己复述，最好**在答辩前亲手跑一遍复现步骤**
- **提前想好一句话回答"哪部分是你自己做的、哪部分是 AI 做的"**。诚实回答，重点说你做了哪些判断和取舍（选题、技术选型、缺陷定位的方向判断、验收标准）

---

## 四、演示视频建议（3 分钟）

PPT 只管答辩自述，演示视频单独录。建议按业务闭环的先后顺序录屏：

| 时间 | 内容 |
|---|---|
| 0:00–0:20 | 打开首页，简介系统定位 |
| 0:20–1:00 | **熔池监测页**：上传三种不同类别的图像，各展示一次分类结果与 ROI 回显 |
| 1:00–1:40 | **质量预测页**：录入一组工艺参数，展示能量密度、RF/SVR 对比与特征重要性 |
| 1:40–2:20 | **异常预警页**：分别选一个正常任务（job 1）和一个异常任务（job 0），对比时序图上的异常区间 |
| 2:20–2:50 | **历史记录页**：展示前面操作产生的记录已入库 |
| 2:50–3:00 | 收尾：一句总结 |

> 录屏前先双击 `一键启动.bat` 拉起服务，确认三个模型都加载成功（`/api/health` 返回 `models_loaded` 全为 true）。
