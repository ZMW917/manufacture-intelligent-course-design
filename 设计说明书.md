# 基于机器视觉与深度学习的激光增材制造（LPBF）熔池在线监测与成形质量智能预测系统

**设计说明书**

| 项目 | 内容 |
|---|---|
| 课程 | 《制造智能技术》课程设计 |
| 班级学号姓名 | 智造 24-1-28 邹明吾 |
| 开发方式 | Vibe Coding（Harness：Claude Code；编程大模型：DeepSeek-v4） |
| 代码规模 | 后端与算法 1298 行（11 个 Python 文件）、前端 851 行（8 个文件）、测试与脚本 741 行 |
| 自建数据集 | <https://huggingface.co/datasets/zoumingwu/lpbf-melt-pool-quality-dataset> |

---

## 1 背景调研

### 1.1 增材制造的质量控制难题

激光粉末床熔融（Laser Powder Bed Fusion，LPBF）以高功率激光按切片路径逐层熔化金属粉末、层层堆积成形零件，是当前金属增材制造中应用最广的工艺之一。它的优势在于能直接成形传统工艺难以加工的复杂内流道与轻量化结构，已在航空发动机零件、医疗植入体、模具随形冷却水道等场合落地。

但 LPBF 的成形质量对工艺窗口极为敏感。激光功率、扫描速度、层厚、扫描间距四个参数共同决定了输入熔池的能量密度：

$$E = \frac{P}{v \cdot t \cdot h}$$

其中 $P$ 为激光功率（W），$v$ 为扫描速度（mm/s），$t$ 为层厚（μm），$h$ 为扫描间距（μm），$E$ 的单位为 J/mm³。能量密度偏低时粉末不能完全熔化，产生未熔合缺陷与层间裂纹；能量密度偏高时熔池过热，容易形成匙孔并卷入气体，冷却后在零件内部留下气孔。这两类缺陷都会显著降低零件的疲劳寿命，而它们恰恰发生在成形过程当中，一旦零件成形完毕，内部缺陷很难无损检出，往往只能整件报废。

更麻烦的是，缺陷的产生不是简单的"参数落在区间外"就能解释的。同样的参数组合，粉末批次、舱内氧含量、基板温度的波动都可能让结果不同。这就要求质量控制不能只靠事前的参数设定，还需要在成形过程中实时观测熔池状态——这正是熔池在线监测的出发点。

### 1.2 熔池监测的主要手段

工业上观测熔池的传感器主要有三类。同轴高速相机通过分光镜与激光共用光路，可获得熔池的形貌图像，直观反映熔池的大小、形状与稳定性；高温计测量熔池辐射亮度以反推温度；氧含量传感器监测成形舱内的气氛，氧含量过高会加剧氧化与球化。这些信号随时间快速变化，人工盯屏既不可靠也不经济，必须交由算法处理。

从算法角度看，这三种信号对应三类不同的任务：图像是空间信号，适合用卷积神经网络提取形貌特征做缺陷分类；工艺参数是静态表格数据，适合用回归模型建立"参数—性能"的映射；温度与氧含量是一维时序，适合用异常检测方法识别过程失稳。这三类任务的技术路线差异很大，把它们放进同一个业务闭环里，恰好覆盖了《制造智能技术》课程的多个技术专题。

### 1.3 本设计的研究意义

本设计要回答的问题是：能否构建一套完整的软件系统，把"上传熔池图像 → 判定质量等级 → 录入工艺参数 → 预测成形致密度 → 监测传感器时序 → 异常预警 → 记录留痕"这条链路真正打通，并且让每个算法环节都在系统中承担实际职责，而不是各自独立、互不相干地堆在一起。

需要说明本设计的定位与边界。真实的 LPBF 产线数据属于企业核心资产，课程设计条件下无法获取；公开基准数据集（如 NIST AM-Bench 2022）虽然真实，但体积大、格式复杂，且需要与具体设备标定对应。因此本设计采用**程序合成的数据集**完成算法链路与系统集成的验证，把工作重点放在"系统架构是否完整、业务闭环是否走得通、算法模块是否真正被使用"上，而非追求模型在真实产线上的精度指标。合成数据集的物理合理性（按能量密度区间生成质量标签、按缺陷成因设计形态）是设计时必须交代清楚的，这一点在第 3 章详述。

---

## 2 方案设计

### 2.1 需求分析

系统面向 LPBF 质量控制场景，围绕四个功能模块展开：

| 编号 | 功能模块 | 业务描述 |
|---|---|---|
| F1 | 熔池图像在线监测 | 用户上传熔池图像，系统完成图像预处理与 ROI 提取，调用 CNN 判定质量等级（致密/气孔/裂纹未熔合）与置信度，并回显预处理后的熔池区域 |
| F2 | 工艺参数质量预测 | 录入四个工艺参数，系统派生能量密度特征，用随机森林与 SVR 分别预测致密度与孔隙率，两种模型结果并列展示便于对比 |
| F3 | 传感器时序异常检测 | 选择打印任务，系统对熔池温度与氧含量时序做异常检测，标记异常点与异常区间，给出正常/注意/告警三级预警 |
| F4 | 历史记录查询 | 分页查询预测历史与图像分类记录，含关键字段与时间戳，作为质量追溯的依据 |

非功能方面，考虑到本设计需要在答辩现场演示，明确要求系统能脱离网络在本地完整运行：不依赖任何在线模型服务，不下载预训练权重，训练所产物随仓库提交，双击启动脚本即可拉起全部服务。同时要求配套的自动化测试在不依赖真实训练权重的情况下也能通过，以保证他人拿到代码后可以验证。

### 2.2 总体架构

系统采用 B/S 架构，分为四层：

```
浏览器（Vue 3 + Element Plus + ECharts）
    │  HTTP / JSON（开发期经 Vite 代理转发到 8000 端口）
    ▼
FastAPI 服务层（src/main.py，7 个 REST 接口，自动生成 Swagger 文档）
    ├── /api/classify   → 算法层：image_preprocess + CNNClassifier
    ├── /api/predict    → 算法层：QualityRegressor（RF / SVR）
    ├── /api/anomaly    → 算法层：AnomalyDetector（IsolationForest + SPC）
    └── /api/history/*、/api/sensors/jobs → 数据层
    ▼
数据层（SQLite 三张表：process_records / prediction_history / image_records）
```

选择 B/S 而非 C/S，主要是考虑到演示与讲解的便利：浏览器打开即可用，无需安装客户端；FastAPI 自带 Swagger 文档，答辩时可以现场展开接口说明；前端用 ECharts 画时序曲线与异常区间，比桌面 GUI 框架更省事。这些取舍在方案论证阶段做过对比，此处不再重复。

### 2.3 算法模块的输入输出契约

并行开发时最容易出问题的地方是模块之间的接口。为避免各模块各自为政，开发前先把三个算法模块的输入输出签名固定下来：

| 模块 | 输入 | 输出 |
|---|---|---|
| `preprocess_image(image)` | H×W 或 H×W×3 的 numpy 数组 | `{'gray','denoised','roi','roi_bbox','enhanced'}` |
| `CNNClassifier.predict(image)` | 原始熔池图像（数组或路径） | `{'label','label_name','confidence','probs'}` |
| `QualityRegressor.predict(params)` | 四个工艺参数的 dict | `{'density_rf','density_svr','porosity_rf','porosity_svr','energy_density_J_mm3','feature_importances'}` |
| `AnomalyDetector.detect(df)` | 单任务的时序 DataFrame | `{'time_step','melt_pool_temp_C','oxygen_ppm','anomaly_indices','intervals','level','threshold_upper','threshold_lower'}` |

需要特别指出 `CNNClassifier.predict` 的输入契约：它接收的是**原始图像**，预处理在函数内部完成。这个约定是在调试阶段吃过亏之后才明确下来的，详见第 6.2 节。

### 2.4 技术方向覆盖说明

任务书要求覆盖《制造智能技术》课程至少 3 个技术方向，且仅装饰性引入不计入覆盖。本设计覆盖 4 个方向，各方向在系统中的实际职责如下：

| 技术方向 | 具体方法 | 在系统中的实际作用 | 对应代码 |
|---|---|---|---|
| 计算机视觉/机器视觉 | OpenCV 灰度化、高斯去噪、Otsu 阈值分割、ROI 提取、直方图均衡 | 为 CNN 提供标准化的熔池区域输入；同时把 ROI 编码为 base64 供前端回显 | `src/algorithms/image_preprocess.py` |
| 深度学习 | ResNet-18（改 1 通道输入、3 分类输出，迁移学习） | 由熔池图像判定成形质量等级，是 F1 模块的决策核心 | `src/algorithms/cnn_classifier.py` |
| 机器学习 | 随机森林、支持向量回归（SVR） | 由工艺参数预测致密度与孔隙率，是 F2 模块的决策核心；随机森林同时输出特征重要性用于解释 | `src/algorithms/regression.py` |
| 数据挖掘 | 孤立森林 + 统计过程控制（3σ 规则） | 对传感器时序做无监督异常检测并分级预警，是 F3 模块的决策核心 | `src/algorithms/anomaly_detector.py` |

其中机器视觉方向需要特别说明：它并非只用于前端展示。CNN 的**训练与推理都经过同一条预处理链路**，模型实际接收的是 ROI 提取并增强后的图像。第 6.2 节的调试记录表明，如果摘掉这一环，模型准确率会从 0.99 崩到 0.05——这从反面证明了该方向承担的是实质性职责。

---

## 3 数据集构建

### 3.1 数据来源

本设计的数据分两部分。**自建数据集**是系统实际使用的数据，由 `scripts/generate_sample_data.py` 程序合成，已开源至 Hugging Face：

<https://huggingface.co/datasets/zoumingwu/lpbf-melt-pool-quality-dataset>

**公开基准数据集**作为真实场景验证的预留路径，链接如下，需自行下载，不随仓库提交：NIST AM-Bench 2022（<https://www.nist.gov/ambench>）提供经标定的 LPBF 熔池几何、温度场与成形性能数据；公开 LPBF 熔池监测数据集可在 Zenodo（<https://zenodo.org>）检索 `LPBF melt pool monitoring dataset` 获取。本设计未使用任何私有敏感数据。

### 3.2 合成数据的设计依据

合成数据的关键在于让数据的生成规则对应真实的物理成因，否则算法验证就失去意义。本设计按下述三条规则构造数据。

**（1）工艺参数与能量密度。** 四个工艺参数独立均匀采样，取值范围参考 316L 不锈钢 LPBF 的常见工艺窗口：激光功率 180–320 W、扫描速度 600–1200 mm/s、层厚 30–60 μm、扫描间距 70–120 μm。体能量密度按 $E = P/(v \cdot t \cdot h)$ 派生，实测落在 27.70–185.32 J/mm³。

**（2）质量标签与缺陷成因的对应。** 标签由能量密度分区决定，并带有随机性以模拟实际过程的波动：

- $40 \le E \le 120$ J/mm³（工艺窗口内）：以 0.8 概率标为致密，0.2 概率标为气孔；
- $E < 40$ J/mm³（能量不足）：以 0.7 概率标为裂纹/未熔合，0.3 概率标为气孔；
- $E > 120$ J/mm³（能量过高）：以 0.7 概率标为气孔，0.3 概率标为致密。

致密度按标签分层采样：致密类落在 0.9950–0.9999，气孔类落在 0.9500–0.9900，裂纹/未熔合类落在 0.8800–0.9500。

**（3）熔池图像的形态。** 图像为 64×64 单通道灰度图，在随机抖动的中心位置叠加椭圆亮度斑模拟熔池，再叠加 σ=6 的高斯噪声。三类的形态按缺陷成因区分：

- 致密类：规整椭圆亮斑，内部均匀；
- 气孔类：在亮斑内部挖出 3–6 个暗色圆形孔洞，模拟气体在熔池内形成的球状气孔；
- 裂纹/未熔合类：亮斑更细长（半轴 a∈[6,12]、b∈[18,26]），模拟未熔合导致的狭长熔池。

这一设计在初版中是有缺陷的——初版只对裂纹类改变了椭圆形态，气孔类与致密类用的是同一组参数，导致两类在像素层面完全不可分。该问题的定位与修复过程见第 6.1 节。

### 3.3 数据规模与分布

| 数据 | 规模 | 说明 |
|---|---|---|
| 熔池图像 | 200 张（64×64 灰度 PNG） | 与工艺参数表通过 `sample_id` 一一对应 |
| 工艺参数表 | 200 条 × 8 列 | 四个工艺参数、能量密度、致密度、质量标签 |
| 传感器时序 | 600 条 × 4 列 | 6 个打印任务 × 100 时间步的温度与氧含量 |
| 预处理后参数特征表 | 197 条 × 19 列 | 3σ 清洗剔除 3 条离群样本后经特征工程与标准化 |
| 传感器聚合特征表 | 6 条 × 8 列 | 按任务聚合的统计量及 3σ 异常标记 |

质量标签分布为致密 151 条（75.5%）、气孔 39 条（19.5%）、裂纹/未熔合 10 条（5.0%），存在明显的类别不平衡。这个分布本身是符合实际的——正常工艺窗口内的合格件占多数，这与第（2）条标签生成规则中工艺窗口内 0.8 的致密概率是一致的。但类别不平衡会给训练带来影响，第 6.3 节对此有量化讨论。

传感器时序中，`job_id` 满足 `job % 3 == 0` 的任务（即 job 0 与 job 3）在第 45–49 时间步注入了幅度 600 °C 的温度尖峰，作为异常检测的阳性样本；其余四个任务为正常工况。

### 3.4 数据预处理

预处理脚本 `scripts/preprocess.py` 完成四件事：删除含缺失值的样本；对激光功率、扫描速度、能量密度按 3σ 规则剔除离群点（剔除了 3 条，200→197）；构造派生特征；对数值特征做 Z-score 标准化。

派生特征的计算方式如下：

| 特征 | 计算式 |
|---|---|
| `power_density` | $P/(v \cdot h \cdot 10^{-6})$ |
| `line_energy_J_mm` | $P/v$ |
| `log_energy_density` | $\ln(1+E)$ |

传感器侧则按任务聚合出温度均值、标准差、极值、氧含量均值，并用 3σ 规则标记超限点。需要说明的是，这一层 3σ 标记与后端 `AnomalyDetector` 的判据并不完全相同：预处理脚本只用单一 3σ 规则，而后端在这个规则之外并联了孤立森林，并按异常点数做三级分档。实测中 job 2（正常任务）在 3σ 规则下出现 1 个超限点，属于 100 点序列上的统计误报（正常序列在 3σ 下的期望误报数约 0.27 个），后端因为采用了更保守的分档阈值仍将其判为"正常"。这个差异是有意保留的：预处理产物用于离线分析，后端用于在线预警，后者对误报更敏感。

### 3.5 数据集的开源

自建数据集以 CC BY 4.0 协议发布于 Hugging Face，仓库内含数据集卡片，说明了数据性质（合成、非真实）、目录结构、字段含义、标签分布、使用方式与局限性。开源仓库的目录结构与本地 `data/` 保持一致，便于对照。

需要明确的是，数据集卡片中写明了四条局限性：数据为合成数据、分布由生成脚本的假设决定，训练所得模型**不可直接迁移到产线**；图像形态为简化模型，未包含真实熔池的飞溅、羽流、匙孔等复杂形貌；类别分布不平衡；传感器异常为人为注入的单点尖峰，未覆盖漂移、阶跃等更复杂的失效模式。把这些写清楚，比让使用者误以为拿到的是真实数据要负责得多。

---

## 4 技术方向详细设计

### 4.1 机器视觉：熔池图像预处理与 ROI 提取

**设计目标。** 原始熔池图像存在三个问题：熔池只占画面一小部分，背景占了大半；图像含高斯噪声；熔池亮斑与背景的对比度不足。预处理的职责是把熔池区域准确地裁剪出来并增强对比度，让后续网络专注于熔池本身，而不是学习背景的无关特征。

**处理流程。** `preprocess_image()` 依次执行四步：

1. **灰度化。** 统一转成 8-bit 单通道。对彩色图按 BGR/BGRA 转灰度；对浮点图，若最大值不超过 1 则视为已归一化到 [0,1] 直接乘 255，否则按 [min, max] 线性映射到 [0,255]。这一步保证了接口能接受多种输入格式而不崩溃。
2. **高斯去噪。** 使用 5×5 核的 `cv2.GaussianBlur`。选高斯而非中值滤波，是因为噪声本身服从高斯分布，且高斯核在抑制噪声的同时对亮斑边缘的模糊更轻微。
3. **Otsu 阈值分割与 ROI 提取。** 用 `cv2.THRESH_OTSU` 自动确定阈值，把熔池亮斑与背景分离，然后取前景像素的外接矩形作为 ROI。这里设了一个安全阀：若分割出的 ROI 小于 16×16 像素，或者根本没有前景像素（全黑图），就判定分割不可靠，回退到使用整幅图像。这个回退分支在处理退化输入时是必要的，否则会把一小块噪声当熔池裁出来送进网络。
4. **直方图均衡增强。** 对 ROI 调用 `cv2.equalizeHist` 拉伸对比度。

**与 CNN 的衔接。** 预处理产出的 `enhanced` 图像是 CNN 的实际输入。这里有一个必须强调的实现细节：`_gray_to_tensor()` 在把 ROI 送进网络前，采用**等比缩放 + 居中补零**而不是直接 resize 成正方形。原因是 ROI 的大小随熔池尺寸变化（实测高度 27.0±6.5、宽 33.7±5.1），而熔池的长宽比本身承载着类别信息——裂纹/未熔合类的熔池是细长的。如果直接硬拉到 64×64，长宽比会被抹平，细长的熔池会变成普通圆斑。第 6.3 节记录了这个问题的完整定位过程。

**ROI 的前端回显。** 预处理得到的 ROI 还会被 `encode_base64()` 编码成 base64 PNG 返回给前端，用户上传图像后能在页面上同时看到原图和系统实际"看到"的熔池区域。这个回显不只是视觉效果，它让用户能判断 ROI 提取是否失败——如果框错了位置，分类结果自然不可信。

### 4.2 深度学习：ResNet-18 熔池质量分类

**模型改造。** 直接使用 `torchvision.models.resnet18`，但做了三处改造以适配本项目：第一，把首层卷积 `conv1` 由 3 通道改为 1 通道，因为熔池图像是单通道灰度图；第二，把全连接层 `fc` 的输出维度从 1000 改为 3，对应三个质量等级；第三，构造时传入 `weights=None`，即随机初始化、不下载预训练权重。第三点是被迫的也是必要的：答辩现场可能没有网络，如果依赖在线下载权重，系统在断网环境下直接起不来。

不加载预训练权重意味着模型要从 200 张图上从头学习，这对 ResNet-18 这样的深层网络并不理想。但本项目的图像是形态规则的合成图，类间差异明确，实测 8 轮训练即可收敛。

**输入标准化。** 图像经 `_gray_to_tensor` 处理：等比缩放补零到 64×64 后，像素先除以 255 归一化到 [0,1]，再按均值 0.5、方差 0.5 标准化到 [-1,1]。这里的均值方差用的是固定常数而非训练集统计量，是一种简化处理。对于自然图像这样做不够严谨，但本项目的图像来自合成、分布稳定，固定常数与统计量的差异可以忽略。

**训练策略。** 固定随机种子 42 保证可复现；按 80/20 划分训练集与验证集（先打乱索引再切分）；损失函数用交叉熵，优化器用 Adam，学习率 1e-3；批大小 16，训练 8 轮。每轮打印损失、训练准确率与验证准确率，并**保存验证集准确率最优的权重**而非最后一轮的权重——这个细节在类别不平衡且样本量小的情况下尤其重要，因为末轮可能恰好处于波动低谷。

**训练结果。** 8 轮训练的验证集准确率依次为 0.9250、0.9500、0.9500、1.0000、0.9500、1.0000、0.9250、0.9250。第 4 轮首次达到 1.0000，第 6 轮再次达到 1.0000；由于保存条件是"验证集准确率不低于历史最优"，第 6 轮的权重覆盖保存，成为最终模型（第 7、8 轮准确率回落到 0.9250，未触发保存）。这也说明验证准确率只有 40 个样本，轮间波动较大。

被保存的模型在全部 200 个样本上评估，整体准确率 0.9900，平均置信度 0.9997，逐类召回率为：致密 1.0000、气孔 0.9487、裂纹/未熔合 1.0000。

需要诚实指出两点。第一，这个 0.9900 是在包含训练样本的全部 200 条数据上评估的，**不是独立测试集结果**，真实泛化能力应以验证集的 1.0000 为准，而验证集只有 40 个样本，统计意义有限。第二，气孔类有 2 个样本被误判为致密（召回 0.9487），这在 39 个样本中占比约 5%，与类别不平衡有关。

### 4.3 机器学习：随机森林与 SVR 工艺参数回归

**任务定义。** 输入四个工艺参数，输出成形致密度。孔隙率不单独建模，而是按 `孔隙率 = 1 - 致密度` 派生——数据集中没有孔隙率列，这样处理既避免了冗余建模，也保证了两个输出在物理上的自洽。

**特征构造。** 在四个原始工艺参数之外，补充派生的体能量密度，共 5 维特征。补充这一维是有依据的：从生成规则看，质量标签本就由能量密度分区决定，把能量密度显式作为特征是给模型提供先验。实测结果印证了这一点——随机森林给出的特征重要性中，能量密度占 0.7182，其余四个原始参数分别为扫描速度 0.1085、扫描间距 0.0648、层厚 0.0586、激光功率 0.0499，均在 0.11 以下。模型确实主要依赖这一派生特征做判断，这与工艺物理是一致的。

**SVR 的目标标准化。** SVR 在实现上有一个必须处理的陷阱。致密度的量级很小，均值约 0.988、标准差仅 0.021，而 SVR 的默认不敏感带 `epsilon=0.1` 远大于这个量级。这意味着所有样本都落在不敏感带内，损失恒为零，SVR 会退化成输出常数的"模型"——它看起来训练成功了，但完全不具备预测能力。

解决办法是在训练前对目标做 Z-score 标准化，训练后再反变换回原始量纲：

```
y_scaled = (y - y_mean) / y_std      # 训练目标
y_pred   = svr.predict(X) * y_std + y_mean   # 预测后反变换
```

标准化后目标的量级与 `epsilon` 匹配，SVR 才能真正拟合密度随参数的变化。这里保存的是训练集的均值和标准差，预测时复用同一组统计量。

**模型评估与一个方法论问题。** 训练脚本 `train.py` 用 80/20 单次划分评估，报告 RF 的 R² 为 0.0949、SVR 为 0.0077。这两个数字看起来模型几乎无效，但进一步分析发现，**它们是评估方法的问题，不是模型的问题**：

单次划分的测试集只有 40 个样本，而致密度的方差本就很小，R² 在这种条件下极不稳定。改用 5 折交叉验证、把 200 个样本全部利用起来后，结果变为：

| 模型 | 单次 80/20 划分 | 5 折交叉验证 |
|---|---|---|
| 随机森林 | 0.0949 | **0.4719** |
| SVR（含目标标准化） | 0.0077 | 0.1443 |

交叉验证的逐折结果也印证了这一点：随机森林五折的 R² 分别为 0.346、0.465、0.567、−0.390、0.636，其中第四折为负——正是这种折间波动，如果恰好落在单次划分的测试集上，就会得到 0.0949 这样的"假低"结果。

那么 0.4719 是好是坏？可以算一个上界作为参照：按照第 3.2 节的生成规则，给定能量密度后，质量标签仍按概率混合，同一能量密度下的致密度是多峰分布。用"已知真实生成过程时的条件期望"去预测，即贝叶斯最优预测器，其 R² 为 **0.5669**。随机森林的 0.4719 达到了这个上界的 83%，说明模型已经提取出了数据中的绝大部分可解释信号；剩余差距来自模型的估计误差，而 0.5669 与 1.0 之间的差距则是生成过程固有的随机性，任何模型都无法消除。

这个分析也给 SVR 的 0.1443 提供了参照——它只利用了约四分之一的可解释信号，明显弱于随机森林。原因在于 SVR 的 RBF 核在高维小样本下需要精细调参（C、gamma），本项目使用的是默认参数。保留 SVR 的意义在于提供一种原理不同的对照——随机森林是集成学习、SVR 是核方法，两者结果并列展示在前端，便于对比观察。

### 4.4 数据挖掘：孤立森林与 SPC 时序异常检测

**问题特点。** 传感器时序异常检测的困难在于没有标注。生产线上一次异常停机成本很高，不可能为了收集训练数据去人为制造大量异常，所以这是典型的无监督问题。另一方面，纯粹的统计阈值方法（如 3σ）又过于机械：它对整段序列用同一组控制限，无法适应过程本身的漂移。

**双判据并联方案。** 本设计把两种原理不同的方法并联使用，取并集作为异常判定结果：

**孤立森林**基于一个直观的观察：异常点在特征空间中偏离正常样本群，因而更容易被随机切分孤立出来——用随机超平面反复切分数据，异常点平均只需更少的分割次数就能被单独隔离。具体实现上，先对温度序列构造滑动窗口特征，包括原始值、5 点窗口均值与窗口标准差，组成 3 维特征矩阵，再送入 `IsolationForest`。使用窗口统计量而非仅用原始值，是为了捕捉"局部波动加剧"这类异常——有时温度均值正常，但波动幅度明显变大，仅看原始值发现不了。

**SPC 3σ 规则**则对整个序列统计均值与标准差，标记超出 $\mu \pm 3\sigma$ 的点。这里用的是对整段序列的全局统计，而不是逐点滑动，因为本项目的时序是离线批量检测而非在线流式检测。

两者的异常点集合取并集去重后，再用 `_merge_intervals()` 把连续异常点索引合并成闭区间，供前端画高亮区域。

**分级预警。** 按并集异常点数分为三档：少于 3 个点判为"正常"，3–4 个判为"注意"，5 个及以上判为"告警"。阈值的选取参考了注入异常的规模——注入的尖峰恰好是 5 个点。

**关于污染率参数的调整。** 初版实现中孤立森林使用 `contamination='auto'`，让算法自动估计污染比例。实测发现这在近乎纯噪声的序列上会误标 10%–25% 的点为异常，导致**所有任务都被判为告警**，预警功能形同虚设。改为固定 `contamination=0.01` 并重新设定分级阈值后，正常任务与异常任务才清晰分开。这一调整记录在第 6.4 节。

**实测结果。** 对 6 个任务逐一检测，注入异常的 job 0 与 job 3 均判为"告警"，异常点数 5 个，异常区间 `[45, 49]`，与注入位置完全吻合；四个正常任务均判为"正常"。正常任务仍会检出 1 个异常点（来自孤立森林按 1% 污染率的固定配额），但因为低于 3 个点的分档阈值，不影响最终判定。

---

## 5 界面设计

### 5.1 整体布局

前端采用单页应用，左侧为固定宽度的导航栏，右侧为内容区。导航栏顶部是系统名称，下方列出四个功能入口，当前页面对应的菜单项高亮。内容区顶部有统一的标题栏，下方按页面展示具体内容，背景为浅灰以便与白色卡片区分。这个布局是管理系统类应用的常规做法，用户不需要学习成本。

### 5.2 四个功能页面

**（1）熔池监测页。** 页面顶部是拖拽上传区，支持 PNG/JPG/BMP/TIFF 格式，上传前会在前端校验文件类型，避免把非图片文件发到后端。上传后，下方并排展示两张卡片：左侧是用户上传的原始图像，右侧是后端返回的熔池 ROI 图像——后者让用户能看到系统实际分析的区域。再下方是分类结果卡片，用不同颜色的标签展示质量等级（致密绿色、气孔橙色、裂纹红色），配一条置信度进度条，并分三行列出三个类别的概率分布。展示完整的三分类概率而不是只给一个结论，是因为当置信度不高时，用户需要看到概率分布才能判断结果是否可信。

**（2）质量预测页。** 左右两栏布局，左栏是工艺参数表单，包含激光功率、扫描速度、层厚、扫描间距四个数字输入框，均带步进控件与最小值限制；右栏展示预测结果，顶部先给出由参数派生的能量密度，然后用表格并列展示随机森林与 SVR 两种模型的致密度与孔隙率预测值，最后用进度条形式列出随机森林的特征重要性，让用户看到模型做判断时主要依据了哪个参数。

**（3）异常预警页。** 顶部是任务选择器与检测按钮，检测完成后在同一行右侧显示预警等级标签与异常点数量。下方是 ECharts 时序图，双 Y 轴分别对应温度与氧含量两条曲线，温度曲线叠加了两条橙色虚线表示 3σ 上下控制限，异常区间用红色半透明区域高亮。把控制限和异常区间画在同一张图上，用户能直观看出异常点是如何超出控制限的，而不是只看到一个结论性的标签。

**（4）历史记录页。** 上下两张表格分别展示预测历史与图像识别记录。预测历史表包含激光功率、预测致密度、预测孔隙率、所用模型（RF/SVR 标签区分）与创建时间；图像记录表包含图像名称、标签编号、分类结果、置信度与时间。两张表都支持分页，每页 10 条。这两张表对应数据库中的 `prediction_history` 与 `image_records` 两张表，起到了质量追溯的作用。

---

## 6 调试

本章记录开发过程中定位并修复的五个实际缺陷。这五个问题分别位于数据层、集成层、算法层与参数配置层，其中前三个是在本次自查中通过实测发现的，后两个是在详细开发阶段发现并修复的。

### 6.1 缺陷一：气孔类与致密类图像在像素层面不可分（数据层）

**现象。** 训练完成后分类准确率看起来正常（0.81），但检查混淆矩阵发现异常：气孔类的召回率为 **0.0000**——38 个气孔样本全部被预测为致密，没有一个被正确识别；而致密类与裂纹类的召回率都是 1.0000。整体准确率 0.81 完全来自"多数类判对"这一项，是虚高的。

**定位。** 直觉上会怀疑是类别不平衡导致模型偏向多数类，但如果是这个原因，模型至少应该偶尔判对几个气孔样本，而不是一个都不对。于是转而检查数据本身：按标签分组统计图像亮斑外接框的长宽比。

修复前的统计结果如下：

| 类别 | 样本数 | 亮斑长宽比 |
|---|---|---|
| 致密 | 151 | 0.78 ± 0.14 |
| 气孔 | 38 | 0.81 ± 0.13 |
| 裂纹/未熔合 | 11 | 2.10 ± 0.41 |

对致密类与气孔类的长宽比做 Welch t 检验，得到 t = −1.244、**p = 0.218**，两类形态**没有统计学差异**。

**根因。** 回到生成脚本 `generate_sample_data.py`，问题一目了然：

```python
a, b = RNG.uniform(14, 20), RNG.uniform(10, 16)   # 类别 0 与类别 1 共用
if label == 2:                                     # 只有类别 2 改变形态
    a, b = RNG.uniform(6, 12), RNG.uniform(18, 26)
```

生成器只对标签 2（裂纹/未熔合）改变了椭圆形态，标签 0（致密）与标签 1（气孔）用的**是同一个分布**。也就是说，这两类的图像在生成时就没有任何区别——**这是数据设计的缺陷，不是模型的问题，靠调参、换网络、加数据增强都无法解决**。

**修复。** 按气孔的物理成因修改生成器：气孔是气体在熔池内部聚集形成的球状孔洞，因此在椭圆亮斑内部挖出 3–6 个半径 1.5–3.0 像素的暗色圆形区域，位置限制在归一化半径 0.6 以内以保证落在熔池内部。同时给图像合成单独分配一条随机流（种子 2024），使今后单独调整图像形态时不会再连带改变工艺参数与标签的生成序列。

**验证。** 重新生成数据后复查可分性，致密类与气孔类的亮斑框内暗像素占比分别为 0.2140 ± 0.0136 与 0.2875 ± 0.0234，Welch t 检验 t = −18.56、**p = 1.26e−22**，差异极显著。重训后气孔类召回率由 0.0000 提升至 1.0000，全量准确率 0.9900（此时裂纹类召回率降至 0.8000，该问题在 6.2、6.3 节继续处理）。

需要说明的是，这次修复改变了数据，因此需要重新生成、重新训练并重新上传数据集，所有指标随之更新。这也是为什么本报告中的全部数据指标与初版不同。

### 6.2 缺陷二：训练与推理的预处理链路不一致（集成层）

**现象。** 上一问题修复后，离线评估显示全量 200 样本准确率 0.99。但在做端到端冒烟测试、逐个检查预测结果正确性（而不仅仅是检查接口是否返回 200）时发现，随机抽取的样本中出现了明显错判：真实标签为"致密"的 sample_000 被判为"裂纹"，真实标签为"气孔"的 sample_004 被判为"致密"。

**定位。** 离线评估与接口调用的差别值得注意。离线评估脚本直接把图像数组传给 `clf.predict()`，而 `/api/classify` 的实现是：

```python
pre = preprocess_image(img)
input_img = pre.get("enhanced", pre.get("gray", img))
result = cnn_classifier.predict(input_img)   # 送入的是预处理后的增强 ROI
```

接口送入网络的是**预处理后的增强 ROI**，而训练时 `_MeltPoolDataset` 是通过 `cv2.imread` 读入**原始图像**后直接送网络的。两端的输入不是同一种东西。

为验证这个判断，用同一个模型分别按两种输入方式评估全部 200 个样本：

| 输入方式 | 整体准确率 | 致密 | 气孔 | 裂纹/未熔合 |
|---|---|---|---|---|
| 原图（训练时用的） | 0.9900 | 1.0000 | 1.0000 | 0.8000 |
| 增强 ROI（接口实际送入的） | **0.3750** | 0.4570 | 0.0513 | 0.4000 |

接口的实际准确率只有 37.5%，**比"全部判为致密"的 75.5% 基线还差**，熔池监测功能实际上是失效的。

**根因。** 这是并行开发中典型的训练/服务不一致（train-serve skew）。`image_preprocess` 与 `cnn_classifier` 由同一个子代理实现，`train.py` 由另一个子代理实现，实现契约里只约定了各自的输入输出签名，**没有约定两者的衔接关系**——预处理模块的输出是否应该作为分类器的输入，契约中没有写明。集成时双方各自"看起来能跑通"，问题就藏了下来。

这也暴露了测试的盲区：原有的接口测试只断言 HTTP 状态码为 200、返回体含 `density_rf` 等字段，从不检查预测结果是否正确。状态码正常不等于功能正常。

**修复。** 明确 `CNNClassifier.predict()` 的输入契约：**接收原始图像，预处理在函数内部完成**，调用方不再自行预处理。相应地，训练侧的 `_MeltPoolDataset.__getitem__` 也改为读入原图后调用 `preprocess_image()` 取 `enhanced` 再送网络。这样训练与推理都走同一条链路，`main.py` 中的 `preprocess_image` 调用则仅用于生成前端回显的 ROI。

选择"让训练迁就推理"而不是反过来，还考虑了一个教学层面的因素：预处理若不在算法链路中承担职责，机器视觉方向就有沦为装饰性引入的风险，不符合任务书第 4(3) 条的要求。

**验证。** 修复后接口链路的准确率由 0.3750 提升至 0.9500，气孔类召回率回到 1.0000。但这次重训同时暴露出一个新问题——裂纹/未熔合类的召回率降为 0.0000，原因见 6.3 节；两者叠加修复后，最终全量准确率为 0.9900。

同时补了一个回归测试 `test_cnn_predict_matches_training_pipeline`，选取三个类别各 2 个样本，断言预测结果与标签全部一致。为确认这个测试不是摆设，实测了它在缺陷复现时的行为：把原始图绕过预处理直接送入网络，这 6 个样本只判对 2 个（全量准确率由 0.99 跌至 0.05），断言必然失败，说明该测试确实能拦住此类回归。

### 6.3 缺陷三：ROI 拉伸抹平长宽比（算法层）

**现象。** 缺陷二修复后重新训练，整体准确率回到 0.9500，气孔类召回率恢复到 1.0000，但**裂纹/未熔合类的召回率跌到了 0.0000**——10 个样本全部被判为致密。这与缺陷一修复前的表现如出一辙，只是换了一个类别。

**定位。** 对比修复前后可以发现，变化的只有输入——现在送进网络的是 ROI 裁剪图。检查 `_gray_to_tensor`：

```python
gray = cv2.resize(gray, (input_size, input_size), ...)   # 直接拉伸成正方形
```

ROI 是熔池外接矩形，裂纹/未熔合类的熔池细长（实测长宽比 2.36，而致密类为 0.78），外接框也是细长的。把细长的 ROI **硬拉成 64×64 正方形**，长宽比这一判别信息就被完全抹掉了——一个 2.36:1 的长条被拉伸后，与一个 0.78:1 的圆斑在形状上趋于接近。网络失去了区分依据，自然把裂纹类判成致密。

**修复。** 把直接拉伸改为**等比缩放 + 居中补零**：按长边计算缩放比例，等比缩放到目标尺寸内，再放入等大的黑色画布居中。这样长宽比得以保留——细长熔池在画布上依然是细长的，两侧留有黑色背景。

**验证。** 修复后裂纹/未熔合类召回率由 0.0000 恢复至 1.0000，整体全量准确率 0.9900，三类召回率分别为 1.0000、0.9487、1.0000。

这个问题的价值在于它说明了一件事：预处理不是"做得越多越好"。ROI 提取本身是合理的，但裁剪之后如果不注意保持几何比例，反而会破坏掉原始图像中本就存在的判别信息。

### 6.4 缺陷四：SVR 退化为常数（算法层）

**现象。** 回归模型训练完成后，SVR 在两个差异很大的工艺参数组合上给出的致密度预测几乎相同，返回的是一个近似常数的结果。

**定位。** 检查 SVR 的超参数配置，发现使用的是默认的 `SVR(kernel='rbf')`，其中不敏感带参数 `epsilon` 默认为 0.1。而目标变量致密度的标准差仅 0.021（均值 0.988），**整个目标变量的取值范围（约 0.88–1.00）都不到 0.1**。在 SVR 的损失函数中，预测值与真实值之差在 `epsilon` 以内的样本不产生任何损失。既然所有样本的偏差都小于 0.1，损失恒为零，优化过程无从下手，SVR 就退化成了输出常数的"模型"。

**修复。** 在训练前对目标做 Z-score 标准化，使目标量级与 `epsilon` 匹配，预测时再反变换回原始量纲。标准化所用的均值与标准差来自训练集，随模型一并保存，预测时复用，训练/预测保持一致。

**验证。** 修复后 SVR 的 5 折交叉验证 R² 由负值提升至 0.1443（虽然仍弱于随机森林，但已具备预测能力）。相比之下，随机森林因为基于决策树分裂、对目标量级不敏感，未受此问题影响。

### 6.5 缺陷五：孤立森林污染率导致全局误报（参数配置层）

**现象。** 异常检测初版上线后，无论选择哪个任务，系统都报"告警"，预警功能失去区分度。

**定位。** 检查发现孤立森林使用了 `contamination='auto'`。这个参数的语义是"让算法自行估计数据中的异常比例"。但在近乎纯噪声的平稳温度序列上，孤立森林总能把一部分点识别为"更容易被孤立"的，自动估计会给出 10%–25% 的污染率，于是每段序列都会被标出大量异常点，全部任务都落入"告警"档。

**修复。** 改为固定 `contamination=0.01`，即预期约 1% 的点为异常，这更符合实际生产中过程异常属于小概率事件的先验；同时重新设定分级阈值（少于 3 点为正常、3–4 点为注意、5 点及以上为告警）。

**验证。** 修复后注入异常的 job 0 与 job 3 判为"告警"、异常区间为 `[45, 49]`，与实际注入位置完全一致；四个正常任务均判为"正常"。正常任务仍会因固定污染率配额检出 1 个异常点，但低于分档阈值，不影响判定。

这个问题的教训是：`contamination='auto'` 这类"让算法自己决定"的参数，在无监督场景下未必可靠——算法会按照自己的目标函数给出一个"合理"的估计，但这个估计是否符合业务语义，需要人来判断。

### 6.6 自动化测试与集成验证

**测试覆盖。** 测试套件 `tests/` 共 9 个用例，分为算法测试与接口测试两组：

| 用例 | 覆盖点 |
|---|---|
| `test_preprocess_image_returns_valid_roi` | 预处理返回 ROI，尺寸不小于 16×16，`roi_bbox` 合法且不越界 |
| `test_cnn_predict_matches_training_pipeline` | 三类各 2 个样本预测正确，守护训练/推理预处理一致性 |
| `test_regressor_predict_density_range` | 致密度落在 [0.8, 1.0]，孔隙率等于 1 减致密度；模型缺失时跳过 |
| `test_anomaly_detector_detect_spike` | 注入尖峰后能检出异常，预警等级不为"正常"，上下限关系正确 |
| `test_health` | 健康检查返回 200 且含状态字段 |
| `test_predict` | 未训练返回 400 且带中文提示，已训练返回 200 且含预测字段 |
| `test_anomaly` | 异常检测接口返回预警等级与异常点列表 |
| `test_history_predictions` | 历史查询返回分页结构 |
| `test_sensors_jobs` | 任务列表返回 [0,1,2,3,4,5] |

测试设计上考虑了"他人拿到代码也能验证"这一要求：所有用例都不依赖网络，模型权重缺失时相关用例自动跳过而非失败，预处理与异常检测的用例则完全不依赖训练产物。最终执行结果为 **9 个用例全部通过**。

**端到端验证。** 除单元测试外，还对全部 7 个接口做了端到端冒烟测试，并按业务闭环逐项核对：

- 健康检查返回三个模型均已加载；
- 分类接口对三个类别各取 2 个样本，**6 个样本全部预测正确**，置信度均在 0.97 以上，ROI 图像正常回显；
- 预测接口在参数 280 W / 900 mm/s / 50 μm / 100 μm 下派生能量密度 62.2222 J/mm³，返回 RF 与 SVR 的致密度与孔隙率，特征重要性排序中能量密度居首（0.7182）；
- 异常检测接口对 6 个任务的判定结果与注入情况完全吻合；
- 历史查询与任务列表接口均正常返回。

这轮冒烟测试与缺陷二发现之前的那轮有一个重要区别：**这次不仅检查接口是否响应，还检查了预测结果是否正确**。正是这一点让缺陷二暴露出来。

---

## 7 总结与展望

### 7.1 完成情况

本设计完成了一套 B/S 架构的 LPBF 熔池在线监测与成形质量智能预测系统，实现了从图像上传、熔池监测、质量预测、异常预警到历史记录留痕的完整业务闭环。系统包含 7 个后端接口、4 个前端页面、SQLite 三张数据表，以及覆盖机器视觉、深度学习、机器学习、数据挖掘四个课程技术方向的算法模块。全部代码在本地离线环境可运行，配套 9 个自动化测试用例全部通过。

数据集方面，构建了含 200 张熔池图像、200 条工艺参数记录与 600 条传感器时序的自建数据集，并以 CC BY 4.0 协议开源至 Hugging Face，附完整的数据集卡片说明数据性质、字段含义与局限性。

### 7.2 主要收获

**第一，数据质量问题的优先级高于模型调优。** 本次开发中耗时最多、影响最大的缺陷是气孔类与致密类图像在生成时就没有区别（第 6.1 节）。在这个问题面前，任何模型层面的努力都是徒劳的——因为模型面对的是一个信息上不可解的问题。这让我理解了为什么工程实践中"数据决定了上限，模型只是逼近这个上限"这句话是对的。后来做的贝叶斯上界分析（第 4.3 节）也印证了这一点：随机森林已经达到了理论上界的 83%，继续调参的空间有限。

**第二，模块接口的对齐比模块本身的质量更容易出问题。** 缺陷二（第 6.2 节）中，预处理模块和分类模块各自的实现都没有错，错的是两者之间的衔接——契约里写了各自的输入输出，却没写两者如何组合。这类问题在单人串行开发时几乎不会发生，但在并行开发中很容易出现，而且双方各自测试都能通过，只有集成时才暴露。

**第三，测试只断言"没报错"是不够的。** 缺陷二之所以能潜伏到自查阶段，直接原因是原有的接口测试只检查 HTTP 状态码和字段是否存在，从不检查预测值是否正确。修复后补充的 `test_cnn_predict_matches_training_pipeline` 则直接断言预测标签与真实标签一致——这类断言才能真正守住功能。

**第四，"让算法自己决定"的参数需要业务校验。** 孤立森林的 `contamination='auto'` 在算法层面是合理的自适应机制，但在本项目的业务语义下导致了全局误报（第 6.5 节）。算法给出的"合理"估计是否符合业务预期，最终需要人来判断。

### 7.3 存在的不足

**其一，模型未在真实数据上验证。** 全部指标都来自合成数据集。合成数据的分布由生成规则决定，与真实 LPBF 过程的统计特性存在差距。虽然设计中预留了 NIST AM-Bench 2022 的接入路径（数据接口按真实数据的字段结构设计，`data/README.md` 中给出了获取方式），但尚未实际跑通。因此，**本系统的模型不具备直接迁移到产线的能力**。

**其二，合成数据的图像形态过于简化。** 真实熔池图像包含飞溅、金属蒸气羽流、匙孔振荡等复杂现象，本项目的椭圆亮度斑模型远不能覆盖。这也是神经网络在合成数据上能轻松达到 0.99 准确率、而这个数字不具备参考价值的原因。

**其三，类别不平衡未做处理。** 裂纹/未熔合类仅 10 个样本（占 5.0%），其召回率指标的统计意义有限。可采用类别加权损失、过采样或数据增强等手段缓解，本次未实施。

**其四，SVR 性能明显弱于随机森林。** 5 折交叉验证下 SVR 的 R² 为 0.1443，仅为随机森林（0.4719）的三分之一。原因是使用了默认超参数而未做调优。保留 SVR 的价值主要在于提供原理不同的对照。

**其五，缺少数据漂移监测。** 系统记录预测历史，但不检测输入数据分布的变化。实际产线中粉末批次、设备状态的变化都会引起分布漂移，模型性能会随时间衰减，需要配套的监测与再训练机制。

### 7.4 展望

后续可从三个方向推进。**数据层面**，接入 NIST AM-Bench 2022 等公开真实数据集，或在有条件时采集真实设备的熔池图像，重新训练并评估模型的真实泛化能力。**算法层面**，针对类别不平衡引入加权损失，对 SVR 做超参数搜索，并尝试用时序模型（如 LSTM）替代目前的滑窗统计特征方案，以更好地捕捉温度序列的时序依赖。**系统层面**，补充数据漂移监测与模型再训练流程，并考虑把 SQLite 替换为支持并发写入的数据库、把模型推理改为异步任务，以适应多用户并发的产线部署场景。

---

## 参考文献

[1] 张智海, 等. 制造智能技术基础[M]. 北京: 清华大学出版社, 2022.

[2] 杨杰. 人工智能基础[M]. 北京: 机械工业出版社, 2025.

[3] 邹明吾. LPBF 熔池监测与成形质量合成数据集[DB/OL]. Hugging Face, 2026. <https://huggingface.co/datasets/zoumingwu/lpbf-melt-pool-quality-dataset>

[4] National Institute of Standards and Technology. Additive Manufacturing Benchmark (AM-Bench) 2022[DB/OL]. <https://www.nist.gov/ambench>

[5] He K, Zhang X, Ren S, et al. Deep Residual Learning for Image Recognition[C]// Proceedings of the IEEE Conference on Computer Vision and Pattern Recognition (CVPR), 2016: 770-778.

[6] Liu F T, Ting K M, Zhou Z H. Isolation Forest[C]// Proceedings of the 8th IEEE International Conference on Data Mining (ICDM), 2008: 413-422.

[7] Breiman L. Random Forests[J]. Machine Learning, 2001, 45(1): 5-32.

[8] Cortes C, Vapnik V. Support-Vector Networks[J]. Machine Learning, 1995, 20(3): 273-297.

[9] Montgomery D C. Introduction to Statistical Quality Control[M]. 7th ed. Hoboken: John Wiley & Sons, 2013.

[10] DebRoy T, Wei H L, Zuback J S, et al. Additive manufacturing of metallic components — Process, structure and properties[J]. Progress in Materials Science, 2018, 92: 112-224.
