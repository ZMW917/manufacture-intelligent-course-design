# 制造智能技术课程设计学习笔记

## 1. AI工具学习与Vibe Coding认知

### 1.1 什么是Vibe Coding
Vibe Coding是一种“以AI为编程搭档”的开发方式，强调：
- **任务拆解**：将复杂功能拆分为可独立验证的小任务。
- **提示词工程**：向AI提供明确约束、上下文和验收标准。
- **规格驱动开发**：先写需求文档，再由AI分步实施。
- **人工审查与调试接管**：对AI代码进行审查、测试，必要时人工回滚或修改。

### 1.2 提示词工程要点
- **任务拆解**：把“实现质量预测模块”拆成“数据预处理→特征选择→模型训练→评估→接口封装”。
- **约束说明**：明确输入输出格式、性能要求（如响应时间）、依赖库版本。
- **验收标准**：定义测试用例，如准确率≥90%，或处理100条记录耗时<1s。

### 1.3 上下文工程与规格驱动
- 创建项目规则文件（如`.cursorrules`），统一代码风格和架构约束。
- 让AI先阅读现有代码再生成新代码，避免重复或冲突。
- 需求文档 → AI制定计划 → 分步实现 → 逐项验收，确保可控性。

### 1.4 AI代码的测试与调试策略
- 使用自动化测试（如pytest、JUnit）验证AI生成代码。
- 调试接管策略：回滚到稳定版本、缩小改动范围、构造最小复现用例。

## 2. Harness工具与模型选型

### 2.1 可选Harness工具（AI编程辅助工具）
| 工具名称 | 特点 |
|---------|------|
| ZCode | 企业级代码生成与审查 |
| Trae | 面向工程场景的AI编程助手 |
| Qwen Code | 阿里通义系列代码模型 |
| 通义灵码 | 集成在IDE中的智能补全与重构 |
| Deepseek Harness | 深度求索的代码生成与推理工具 |
| Claude Code | Anthropic Claude的编程特化版 |
| Codex | OpenAI早期代码模型（GitHub Copilot内核） |
| Open Code | 开源代码生成工具集 |

### 2.2 拟选用的模型
结合本人开发环境与任务需求，拟选用以下模型（至少一种主模型，一种备选）：
- **主模型**：Deepseek-V4（代码理解与生成能力均衡，支持本地化部署）
- **备选模型**：Qwen 3.8（轻量、响应快，适合前端与脚本生成）

### 2.3 选择理由
- Deepseek-V4在工业代码生成评测中表现较好，支持中文注释。
- Qwen 3.8适合快速原型开发和前后端联调。

## 3. Git原理与功能

### 3.1 Git核心概念
- **工作区（Working Directory）**：本地文件目录。
- **暂存区（Staging Area）**：`git add`后的临时存储。
- **本地仓库（Local Repository）**：`git commit`后保存的版本历史。
- **远程仓库（Remote Repository）**：如GitHub，用于协作与备份。

### 3.2 常用Git命令及作用
| 命令 | 作用 |
|------|------|
| `git init` | 初始化本地仓库 |
| `git clone <url>` | 克隆远程仓库 |
| `git add <file>` | 将文件加入暂存区 |
| `git commit -m "msg"` | 提交到本地仓库并记录说明 |
| `git push` | 推送本地提交到远程仓库 |
| `git pull` | 拉取远程更新并合并 |
| `git branch` | 管理分支 |
| `git merge` | 合并分支 |
| `git log` | 查看提交历史 |
| `git reset` | 回退版本（用于调试接管） |

### 3.3 本课程Git使用规范
- 小步提交，每次提交附带清晰的变更说明（如“添加数据清洗函数”）。
- 使用`.gitignore`忽略临时文件、数据集、环境变量。
- 分支策略：`main`作为稳定版，`dev`用于日常开发，`feature-*`用于各模块开发。

## 4. 选题调研

### 4.1 选题方向
拟选定主题：**基于机器学习的注塑成型工艺参数优化与质量预测系统**

### 4.2 场景与业务价值
- **场景**：注塑车间生产环节，涉及温度、压力、保压时间、冷却时间等参数。
- **痛点**：参数设置依赖经验，导致废品率高、调试时间长。
- **智能化目标**：利用历史生产数据，预测制品质量（如尺寸精度、表面缺陷），并推荐最优工艺参数。

### 4.3 技术方向映射（覆盖≥3章课程内容）
| 课程技术方向 | 在系统中的实际作用 |
|-------------|-------------------|
| 数据预处理与特征工程 | 清洗注塑工艺原始数据，构造特征（如温度波动、压力峰值） |
| 机器学习（分类/回归） | 构建质量预测模型（如随机森林、XGBoost） |
| 智能优化算法 | 使用遗传算法或贝叶斯优化进行参数寻优 |
| Web后端开发（API） | 使用Flask/FastAPI封装预测与优化服务 |
| 数据库 | 存储历史工艺数据与推荐结果（MySQL/PostgreSQL） |

### 4.4 系统架构草图（初步）
