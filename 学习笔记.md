# 制造智能技术课程设计 · 学习笔记

> 学生：智造24-1-28 邹明吾
> 阶段：D1–D2 vibe coding 学习 + 选题调研与方案设计
> 本文是个人学习过程的真实记录，配合《制造智能技术课程设计任务书》的五个 vibe coding 学习要点逐一梳理。

## 一、对 AI 工具（Vibe Coding）的学习

Vibe coding 本质上是一种"人负责方向与把关、AI 负责执行细节"的编程方式——我用自然语言描述意图，AI 生成代码，我再验收、审查、纠偏。这一轮学习我最大的体会是：**AI 写得快不等于写得对，真正决定成品质量的是我如何约束、喂料和把关。** 下面按任务书要求的五个要点记录心得。

**1. 提示词工程（任务拆解、约束说明、验收标准）**

一开始我犯的错是给 AI 一句"帮我做一个熔池监测系统"这种大而空的需求，结果它东一榔头西一棒子，产出既乱又不可用。后来我学会三件事：一是**任务拆解**，把整个项目拆成"图像上传→预处理→CNN 分类→回归预测→异常检测→数据库→前端"若干可独立交付的子任务，一次只让 AI 做一块；二是**约束说明**，明确技术栈（Vue3 + FastAPI + SQLite、Python 3.10）和边界，不给它自由发挥的空间；三是**验收标准**，每个任务都写清"什么算做完"，比如"输入一张熔池图，返回质量等级与置信度"。验收越可测，AI 越少跑偏。

**2. 上下文工程（项目规则文件、让 AI 先读代码再改）**

这一条给我的触动比提示词还大。我发现 AI 的质量很多时候不取决于我怎么"问"，而取决于它"看到"了什么。我的做法是维护一份 `spec.md` 作为权威上下文，把题目、技术方向映射、技术栈、数据结构都锁死，让 AI 每次干活前先读它，避免它凭空编造背景。改 bug 时我坚持**让 AI 先 Read 相关文件、理解现有代码再动手**，而不是让它凭印象直接改——后者经常"改了一处、坏了一堆"。

**3. 规格驱动开发（需求文档→AI 制定计划→分步实现→逐步验收）**

我采用"先规格、后代码"的路线：先把需求写成规格文档，再让 AI 基于规格制定分步计划，然后一步一步实现、一步一步验收。这样做的好处是需求被"锁定"后不再随意漂移，每一步的产出都能对照规格检验。我把 `spec.md` 标注为"锁定版"，就是为了让后续所有文档与代码生成保持一致，避免写到后面和前面自相矛盾。

**4. AI 代码的自动化测试与人工审查**

AI 生成的代码"看起来对"和"真的对"是两回事。我的原则是：**凡是 AI 生成的模块，必须配 pytest 自动化测试兜底**，让"能不能跑通"变成可机器判定的结果；同时**人工审查逻辑**，重点看它是否符合制造场景、有没有悄悄简化掉关键步骤。任务书也明确禁止"AI 一键生成后未经理解消化的成果"，所以我要求自己对每一段关键代码都能讲清作用与逻辑。

**5. 调试与人工接管策略（回滚、缩小改动、最小复现）**

AI 也有钻牛角尖的时候。我的策略是：**回滚**——靠 git 小步提交，出错就退回上一个稳定版本，而不是在错误基础上继续叠加；**缩小改动**——每次只改一小块，把影响面控制住；**最小复现**——构造最小的输入去复现 bug，再让 AI 针对这个最小用例修复。一旦发现 AI 陷入"越改越乱"的死循环，我就果断人工接管，亲自定位问题，再交给它或自己改。

## 二、拟选用的 Harness 与模型

**Harness：Claude Code。**

我选 Claude Code 的理由有三点：一是它支持项目规则文件（`CLAUDE.md`），恰好契合我上面说的"上下文工程"，能把规格和约定固化进工作流；二是它提供完整的 agent 式工具链（读文件、全局搜索、执行命令、多步编辑、任务拆解），能自主完成"读代码→改代码→跑测试"的循环，而不是简单的对话补全；三是它天然契合"规格驱动开发"这种分步、迭代的工作方式。

对比任务书列出的其他工具：

- **Trae**：字节的 AI IDE，可视化集成和补全体验好，但更偏"对话框 + 代码补全"，对我想要的终端化、可精细控制的 agent 工作流不够灵活。
- **通义灵码 / Qwen Code**：阿里系，中文问答和补全做得不错，但自主多步改代码的 agent 循环成熟度不如 Claude Code。
- **Codex**：OpenAI 出品，补全能力很强，但 CLI 的项目级上下文管理（规则文件、长期记忆）我觉得不如 Claude Code 顺手。

**编程大模型：DeepSeek-v4（deepseek-v4-pro）。**

理由：代码生成与逻辑推理能力扎实、中文理解到位、性价比高，做工业场景的后端与算法代码够用且稳定。对比任务书列出的其他模型：**GLM-5.3** 中文能力强，**Qwen 3.8** 工具调用与 agent 场景表现好，**Kimi K3** 长上下文占优——但综合"代码质量 + 中文理解 + 成本"三者，DeepSeek-v4 对我来说是更均衡的选择。

**本项目实际采用的组合是「Claude Code + DeepSeek-v4」**：Claude Code 承担 Harness 角色，负责 agent 工作流（读代码、执行命令、多步编辑、任务编排），DeepSeek-v4 承担编程大模型角色，负责具体的代码与文档生成。两者分工，前者管"怎么干"，后者管"写出来"，共同完成本项目的文档、代码生成与调试。

## 三、Git 原理与功能

Git 是分布式版本控制系统，核心是**快照**：每次提交都是对当时全部文件状态的一次快照记录，而非简单存差异。它有三个层次——**工作区**（正在改的文件）、**暂存区**（`git add` 后待提交的变更）、**本地仓库**（`.git`，真正存快照的地方）。**提交（commit）** 会形成一个由 hash 标识、指向父提交的有向无环图；**分支**本质是轻量指针，**合并**则把两条线汇合生成合并提交；**回滚**靠 `reset`/`revert`/`checkout` 在不同提交间切换或撤销；**远程协作**通过 `remote + push/pull/fetch` 与远端仓库同步。

落到本项目，我给自己定的规矩是**小步提交 + 有意义的提交说明**：一个功能完成就提交一次（如"feat: 实现熔池图像 ROI 提取"），说明里写清"做了什么、为什么"，而不是笼统的"update"。这样既满足任务书"全程用 git 管理、小步提交"的要求，也让我在调试出错时能精确回滚到任意稳定节点。prompt/会话日志我也会一并纳入版本管理，作为过程档案留存。

## 四、选题调研

**调研过程。** 我先精读任务书，圈定硬性条件：场景必须是制造/工业环节、内容须覆盖课程至少 3 个技术方向、架构须是完整 B/S 或 C/S 可运行 demo。然后我逐个拉取了群里已接龙的 61 个选题仓库的"选题说明"，按题目与技术方向做归类统计，摸清大家都在做什么、哪些方向已经挤成一团。

**61 个已提交选题的方向分布。** 汇总下来最明显的是三大"红海"：**预测性维护/故障诊断（PHM）约 16 人**、**表面缺陷/外观视觉检测约 14 人**、**质量预测/工艺参数优化约 10 人**，三者加起来占了六成多。从技术方向频次看，机器学习出现 35+ 次、深度学习 25+ 次、计算机视觉约 14 次、预测性维护约 12 次，都是高度扎堆的方向。

**为何最终选择「增材制造熔池监测与成形质量预测」。** 理由有三：第一是**避开热门重复**——缺陷检测、预测性维护、质量预测已被大量同学占据，而增材制造（LPBF 金属 3D 打印）在 61 个选题里无人涉足，天然满足"选题不重复"的要求；第二是**制造场景真实**——激光粉末床熔融过程中的熔池监测与成形质量控制是真实工业痛点，且有 NIST AM-Bench 2022 这类公开基准数据可支撑，不是凭空捏造；第三是**能自然覆盖 4 个课程技术方向**——机器视觉（OpenCV 图像预处理与 ROI 提取）、深度学习（CNN 熔池图像质量分类）、机器学习（随机森林/SVR 工艺参数→致密度/孔隙率回归）、数据挖掘（孤立森林 + SPC 传感器时序异常检测），四个方向各有实实在在的作用，不靠"装饰性引入"凑数。

## 五、阶段小结

当前已完成任务书进度安排的前三阶段（D1–D2 动员与 vibe coding 学习、选题调研，D3–D5 选题与方案设计，D6 数据资源整理）。已完成：vibe coding 五要点学习、61 个选题调研分析、`spec.md` 规格锁定、系统架构与技术方向映射确定、数据来源（NIST AM-Bench 2022 + Zenodo 公开数据集 + 本地合成样例）确定，以及项目目录骨架搭建、`generate_sample_data.py` 生成 200 张合成熔池图像与工艺/传感器数据、`preprocess.py` 完成数据清洗与特征工程入库。下一步：进入各算法模块（CNN 分类、RF/SVR 回归、孤立森林异常检测）与前后端的详细开发。
