# 方案设计

## 基于机器视觉与深度学习的激光增材制造（LPBF）熔池在线监测与成形质量智能预测系统

| 项目 | 内容 |
|---|---|
| 学生 | 智造24-1-28 邹明吾 |
| 课程 | 《制造智能技术》课程设计 |
| 题目 | 基于机器视觉与深度学习的激光增材制造（LPBF）熔池在线监测与成形质量智能预测系统 |
| 场景 | 激光粉末床熔融（LPBF）金属 3D 打印过程的质量控制（增材制造） |
| Vibe Coding 工具（Harness） | Claude Code |
| 编程大模型 | DeepSeek-v4（deepseek-v4-pro） |

---

## 一、功能需求分析

### 1.1 功能需求

本项目面向 LPBF（激光粉末床熔融）金属增材制造过程中的质量控制环节，构建一套 B/S 架构的可运行 demo，围绕"熔池图像上传→在线监测→质量预测→异常预警→历史记录"形成完整业务闭环。核心功能模块如下：

| 编号 | 功能模块 | 功能描述 |
|---|---|---|
| F1 | 熔池图像上传与在线监测 | 用户通过前端上传熔池红外/可见光图像，后端完成图像预处理（灰度化、去噪、ROI 提取、增强）后，调用 CNN 分类模型判定成形缺陷/质量等级（致密/气孔/裂纹），返回分类结果与置信度，并在前端回显预处理后的 ROI 图像 |
| F2 | 工艺参数录入 → 成形质量预测 | 用户录入激光功率、扫描速度、层厚、扫描间距等工艺参数，系统派生能量密度等特征，调用随机森林/SVR 回归模型预测成形质量（致密度、孔隙率），返回预测值 |
| F3 | 传感器时序异常检测与预警 | 对熔池温度、氧含量等传感器时序数据进行异常检测，采用孤立森林结合统计过程控制（SPC，3σ 规则）标记异常点，对异常任务生成预警信息 |
| F4 | 历史记录查询与可视化 | 查询预测历史、异常预警记录，以 ECharts 展示质量预测趋势、传感器时序曲线与异常区间，支持列表筛选与详情查看 |

### 1.2 非功能需求

| 类别 | 需求描述 |
|---|---|
| 可运行性 | 后端通过 `uvicorn` 一键启动 FastAPI 服务，前端 `npm run dev` 启动，配套 README 与运行脚本；demo 可脱离外部数据在本地样例数据上完整走通业务闭环 |
| 性能 | 单次图像分类响应时间目标 < 2s（本地推理）；回归预测与异常检测为毫秒级；SQLite 满足单用户演示场景的并发读写 |
| 可测试性 | 采用 pytest 编写单元测试与接口测试，覆盖数据预处理、三个算法模块的输入输出、API 端点与数据库读写；测试不依赖真实工业数据，基于合成样例数据可复现 |
| 可扩展性 | 前后端分层解耦（前端 UI / FastAPI 服务层 / 算法模块 / 数据层），算法模块统一为"输入→处理→输出"的标准化接口，便于后续替换或新增模型、切换数据库 |

---

## 二、方案论证

### 2.1 架构选型：B/S vs C/S

| 维度 | B/S 架构（选定） | C/S 架构 |
|---|---|---|
| 部署与访问 | 浏览器直接访问，无需安装客户端，便于现场演示 | 需安装专用客户端，部署成本高 |
| 任务书契合 | 满足"浏览器/前端 UI + 后端服务 + 数据库"的完整架构要求 | 服务层与 UI 耦合较紧 |
| 可视化 | 前端 ECharts 天然适合图表展示 | 需额外 GUI 框架 |

**结论**：任务书明确要求实现"前端 UI + 后端服务（API 或服务层）+ 数据库 + 算法模块"的完整架构，B/S 架构层次清晰、便于演示与答辩讲解，故选 B/S。

### 2.2 后端框架：FastAPI vs Django/Flask

| 维度 | FastAPI（选定） | Flask | Django |
|---|---|---|---|
| 异步与性能 | 原生异步、基于 ASGI，性能高 | 同步为主 | 同步为主 |
| 自动文档 | 自带 OpenAPI/Swagger 文档，便于答辩展示 API | 需额外插件 | 需额外配置 |
| 类型校验 | Pydantic 请求/响应校验，适合规格驱动开发 | 弱 | 内置 ORM 表单 |
| 学习与体量 | 轻量、上手快，适合 10 天课程设计 | 轻量 | 较重 |

**结论**：本项目算法以 PyTorch/scikit-learn 为主，FastAPI 的异步能力与自动文档契合 vibe coding 规格驱动开发，且体量轻、便于 10 天内快速集成。

### 2.3 数据库：SQLite vs MySQL

| 维度 | SQLite（选定） | MySQL |
|---|---|---|
| 部署 | 零配置、单文件、随仓库提交 | 需独立安装服务端 |
| 场景适配 | 单用户 demo、预测历史记录量小，完全够用 | 面向多用户高并发生产 |
| 可维护性 | 无运维成本，便于演示与测试 | 环境依赖多 |

**结论**：本项目为单用户可运行 demo，历史记录数据量小，SQLite 零配置、可随仓库提交，契合"可现场演示"与"自动化测试"要求，故选 SQLite。

### 2.4 图像分类：CNN vs 传统图像特征

| 维度 | CNN（ResNet-18，选定） | 传统图像特征（HOG/灰度统计+SVM） |
|---|---|---|
| 特征表达 | 自动学习层次化特征，对熔池复杂纹理鲁棒 | 依赖手工特征，泛化能力弱 |
| 课程契合 | 直接对应"深度学习"技术方向 | 仅覆盖"机器视觉"，不足以支撑深度学习方向 |
| 任务书要求 | 满足 ≥3 个技术方向中"深度学习"的实质覆盖 | 无法覆盖深度学习 |

**结论**：任务书要求覆盖至少 3 个技术方向且"仅装饰性引入不计入覆盖"，选用 ResNet-18 做迁移学习，才能实质覆盖"深度学习"方向；传统图像特征则通过 OpenCV 预处理实现"机器视觉"方向的实质覆盖。

### 2.5 回归模型：随机森林/SVR vs 其他回归模型

| 维度 | 随机森林 + SVR（选定） | 线性回归 | 深度回归网络 |
|---|---|---|---|
| 非线性拟合 | 能刻画工艺参数与致密度的非线性关系 | 仅线性，欠拟合 | 能，但样本量小易过拟合 |
| 样本量适配 | 200 条级小样本下稳定、鲁棒 | 简单 | 需大数据量，小样本易过拟合 |
| 可解释性 | 随机森林提供特征重要性，便于答辩讲解 | 高 | 低 |

**结论**：本项目工艺参数样本量有限（数百条），随机森林 + SVR 在小样本下表现稳定、可解释性强，且分别覆盖"机器学习"方向中的集成学习与支持向量回归，故采用二者对比择优输出。

---

## 三、方案规划

### 3.1 技术栈总览

| 层次 | 技术选型 |
|---|---|
| 前端 | Vue 3 + Element Plus（UI 组件）+ ECharts（可视化） |
| 后端 | Python 3.10 + FastAPI（REST API，自带 Swagger 文档） |
| 数据库 | SQLite（单文件数据库） |
| 算法 | PyTorch（CNN/ResNet-18）、scikit-learn（RandomForest/SVR/IsolationForest）、OpenCV（图像预处理）、NumPy、Pandas |
| 测试 | pytest |

### 3.2 需配置的外部资源

| 资源 | 说明 |
|---|---|
| Python 环境 | Python 3.10 虚拟环境，管理依赖（requirements.txt） |
| 依赖库 | fastapi、uvicorn、pydantic、numpy、pandas、scikit-learn、torch、torchvision、opencv-python、pytest 等 |
| Vibe Coding 工具（Harness） | Claude Code（参考任务书第 4 条 Harness 工具） |
| 编程大模型 | DeepSeek-v4（deepseek-v4-pro，参考任务书第 5 条模型） |
| 公开数据集 | NIST AM-Bench 2022（主）、Zenodo LPBF 熔池监测数据集（辅助）、飞桨 AI Studio / 魔搭模型与数据资源 |

### 3.3 数据集来源

依据 `data/README.md`，数据分两部分：

| 数据集 | 来源 | 用途 |
|---|---|---|
| NIST AM-Bench 2022（增材制造基准） | <https://www.nist.gov/ambench>，LPBF 挑战含熔池几何形貌、温度场与成形性能（致密度/缺陷） | 熔池图像分类、工艺参数→致密度回归模型的训练/验证基准 |
| 公开 LPBF 熔池监测数据集（辅助） | Zenodo 检索 `LPBF melt pool monitoring dataset`，或魔搭/飞桨 AI Studio | 传感器时序异常检测模块验证 |
| 本地合成样例数据（随仓库提交） | `scripts/generate_sample_data.py` 生成，非真实企业数据 | 离线 demo 与自动化测试 |

本地样例数据说明：

| 文件/目录 | 说明 |
|---|---|
| `data/raw/images/` | 200 张合成熔池灰度图（64×64，含致密/气孔/裂纹三类形态） |
| `data/raw/process_params.csv` | 200 条工艺参数记录（激光功率、扫描速度、层厚、扫描间距、能量密度、致密度、质量标签） |
| `data/raw/sensor_timeseries.csv` | 6 个打印任务的熔池温度/氧含量时序（含 2 个注入异常任务） |
| `data/processed/features.csv` | 预处理+特征工程+标准化后的参数特征表 |
| `data/processed/sensor_features.csv` | 传感器时序按任务聚合的统计特征与 3σ 异常标记 |

### 3.4 前端 UI 方案

采用单页应用（SPA），左侧导航栏划分 4 个页面：

| 页面 | 交互流程 |
|---|---|
| 熔池监测页 | 选择/拖拽图像 → 点击上传 → 前端回显原图与预处理后 ROI → 展示 CNN 分类结果（质量等级 + 置信度） |
| 质量预测页 | 表单录入工艺参数 → 点击预测 → 展示致密度/孔隙率预测值（RF/SVR 对比）→ 可选保存至历史 |
| 异常预警页 | 选择打印任务 → 展示温度/氧含量时序曲线（ECharts）→ 高亮异常区间与 3σ 上下限 → 展示预警列表 |
| 历史记录页 | 列表展示预测/预警历史 → 筛选、分页 → 点击查看详情 → ECharts 展示质量趋势 |

### 3.5 后端算法方案

每个算法模块统一为"输入→处理→输出"标准化接口，由 FastAPI 路由调用。

**（1）图像分类（CNN/ResNet-18）**

| 环节 | 说明 |
|---|---|
| 输入 | 熔池红外/可见光图像（64×64 灰度图） |
| 处理 | OpenCV 预处理：灰度化 → 高斯去噪 → 阈值分割提取熔池 ROI → 直方图均衡增强；输入 ResNet-18（迁移学习，替换全连接层为 3 类）前向推理，Softmax 输出概率 |
| 输出 | 质量等级（致密/气孔/裂纹）+ 各类置信度 |

**（2）工艺参数回归（随机森林 / SVR）**

| 环节 | 说明 |
|---|---|
| 输入 | 工艺参数：激光功率、扫描速度、层厚、扫描间距（前端表单） |
| 处理 | 特征工程：派生功率密度、线能量、能量密度等特征 → Z-score 标准化（复用训练集均值/方差）→ 随机森林与 SVR 分别预测致密度、孔隙率 |
| 输出 | 致密度预测值、孔隙率预测值（两模型结果对比展示） |

**（3）传感器时序异常检测（孤立森林 + SPC）**

| 环节 | 说明 |
|---|---|
| 输入 | 单打印任务的熔池温度、氧含量时序数据 |
| 处理 | 提取滑动窗口统计特征（均值/方差/极值）→ 孤立森林检测异常样本 → 叠加 SPC 3σ 规则标记超出控制限的点，二者取并集作为异常区间 |
| 输出 | 异常点索引、异常区间、预警等级（正常/注意/告警） |

### 3.6 数据库设计

采用 SQLite，三张表：

**参数记录表 `process_records`**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | 主键 |
| laser_power | REAL | 激光功率 |
| scan_speed | REAL | 扫描速度 |
| layer_thickness | REAL | 层厚 |
| hatch_spacing | REAL | 扫描间距 |
| energy_density | REAL | 派生能量密度 |
| created_at | TEXT | 录入时间 |

**预测历史表 `prediction_history`**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | 主键 |
| record_id | INTEGER FK | 关联参数记录 |
| density_pred | REAL | 致密度预测值 |
| porosity_pred | REAL | 孔隙率预测值 |
| model_type | TEXT | RF / SVR |
| created_at | TEXT | 预测时间 |

**图像记录表 `image_records`**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | 主键 |
| image_path | TEXT | 图像存储路径 |
| quality_label | TEXT | 分类结果（致密/气孔/裂纹） |
| confidence | REAL | 置信度 |
| created_at | TEXT | 上传时间 |

---

## 四、技术路线

分五个阶段推进：

| 阶段 | 内容 | 关键产出 |
|---|---|---|
| 阶段 1：数据准备 | 下载/整理公开数据集，运行 `generate_sample_data.py` 与 `preprocess.py` 生成样例与特征表 | `data/raw/`、`data/processed/` |
| 阶段 2：算法训练 | 用 ResNet-18 训练图像分类；用 RF/SVR 训练回归；用孤立森林+SPC 实现异常检测，保存模型权重 | 三个算法模块（`.pth` / `.joblib`） |
| 阶段 3：后端服务 | FastAPI 定义 REST 接口（图像上传、预测、异常检测、历史查询），对接算法模块与 SQLite | `src/` 后端 + 数据库 |
| 阶段 4：前端 UI | Vue3 + Element Plus + ECharts 搭建四个页面，联调后端 API | 前端页面 |
| 阶段 5：集成测试 | 前后端联调走通完整业务闭环，pytest 覆盖全部模块，编写 README 与运行脚本 | 可演示 demo + 测试报告 |

---

## 五、计划安排

对照任务书 10 天进度安排，细化如下：

| 日期 | 阶段 | 工作内容 |
|---|---|---|
| D1 | 动员、工具配置与 vibe coding 学习 | 配置 Claude Code + DeepSeek-v4 环境，学习提示词工程、上下文工程、规格驱动开发、AI 代码测试与人工审查、调试接管策略 |
| D2 | 调研与选题 | 调研制造/工业智能应用场景，确定 LPBF 增材制造选题，调研 NIST AM-Bench、Zenodo 等数据资源 |
| D3 | 选题与方案设计 | 撰写选题说明（技术方向映射表、系统架构草图、数据来源），群内接龙提交选题 |
| D4 | 方案设计 | 完成需求分析、方案论证与方案规划（本文件），确定技术栈与目录结构 |
| D5 | 方案设计 | 细化数据库表结构、算法输入输出接口、前端页面交互，形成可开发规格 |
| D6 | 数据资源整理 | 下载公开数据集，生成并预处理样例数据，完成数据入库与 `data/README.md` 说明 |
| D7 | 详细开发（一） | 开发算法模块（CNN 分类、RF/SVR 回归、孤立森林异常检测）与后端 FastAPI 服务、SQLite 建表 |
| D8 | 详细开发（二） | 开发前端 Vue3 四个页面，前后端联调走通业务闭环 |
| D9 | 集成调试、报告撰写 | pytest 自动化测试与集成调试，撰写设计报告，录制 3 分钟演示视频 |
| D10 | 答辩 | 制作答辩 PPT，参加答辩（自述 3 分钟 + 问答 7 分钟，准备随机代码段讲解） |

---

## 六、参考文献

1. 张智海 等. 制造智能技术基础[M]. 北京：清华大学出版社，2022.（技术方向知识支撑）
2. 杨杰. 人工智能基础[M]. 北京：机械工业出版社，2025.
3. vibe coding 学习资源：GitHub、CSDN、B 站视频（自行检索学习）。
4. Harness 工具参考：ZCode、Trae、Qwen Code、通义灵码、Deepseek Harness、Claude Code、Codex、Open Code。
5. 编程大模型参考：Deepseek-v4、GLM-5.3、Qwen 3.8、Kimi K3、Claude Code。
6. 模型与数据资源：飞桨 AI Studio（https://aistudio.baidu.com/）；魔搭（https://modelscope.cn）。
7. National Institute of Standards and Technology (NIST). Additive Manufacturing Benchmark (AM-Bench) 2022[DB/OL]. https://www.nist.gov/ambench.
8. Zenodo. LPBF Melt Pool Monitoring Dataset[DB/OL]. https://zenodo.org.
