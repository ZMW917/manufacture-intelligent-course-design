# 制造智能课程设计：基于检索增强生成的大模型智能问答系统

## 摘要

本课程设计旨在开发一个基于检索增强生成（RAG）技术的大模型智能问答系统，面向校园学习场景，以课程教材、教学讲义、校园规章制度等文档构建领域知识库，结合大语言模型实现准确、完整、可追溯的自然语言问答。系统采用LangChain框架开发，以豆包大模型为默认生成模型，通过五环节流水线（文档解析→文本处理→向量化→检索→生成）实现从知识源输入到答案输出的完整链路。设计涵盖数据资源构建、预处理流程、系统开发与质量评估，并建立完善的AI工具提示词追溯体系，满足课程设计的过程留痕与考核要求。


## 1 背景调研与研究意义

### 1.1 技术背景：大模型的突破与局限

大规模预训练语言模型在文本生成、语义理解、知识问答等任务上表现出强大能力。然而，通用大语言模型面向特定领域时存在三大局限：

| 局限类型 | 具体表现 |
|----------|----------|
| 知识更新滞后 | 训练数据有截止日期，无法覆盖最新领域知识 |
| 专业覆盖不足 | 特定领域深度知识缺失，回答不够精准 |
| 容易产生幻觉 | 生成看似合理但实际错误的内容，缺乏可追溯性 |

### 1.2 RAG技术：检索增强生成的核心价值

检索增强生成（Retrieval-Augmented Generation，RAG）通过在模型生成前引入外部知识检索环节，将用户问题与领域知识库中的相关内容进行匹配，显著提升回答的准确性、时效性与可解释性。其核心工作流程如下：

```mermaid
flowchart LR
    A[用户问题] --> B[向量检索]
    C[领域知识库] --> B
    B --> D[相关知识片段]
flowchart LR
    A[1.文档解析] --> B[2.文本处理]
    B --> C[3.向量化]
    C --> D[4.检索]
    D --> E[5.生成]

    F[知识源输入] --> A
    E --> G[答案输出 + 引用]
flowchart LR
    A[资料收集] --> B[内容审核]
    B --> C[文本整理]
    C --> D[问题编写]
    D --> E[答案标注]
    E --> F[交叉核验]

    A1[公开可获取资料] --> A
    B1[脱敏与合规检查] --> B
    C1[格式化与结构化] --> C
    D1[多类型问答覆盖] --> D
    E1[标准答案与来源] --> E
    F1[可回溯验证] --> F
data/
├── public/                    # 公开数据集
│   ├── THUCNews/              # 清华新闻分类数据集
│   ├── LCQMC/                 # 哈工大问句匹配数据集
│   └── README.md              # 来源说明与引用
├── knowledge_base/            # 自建知识库
│   ├── course_materials/      # 课程教材与讲义
│   ├── campus_rules/          # 校园规章制度
│   └── lab_manuals/           # 实验室使用说明
├── qa_pairs/                  # 问答评测对
│   └── qa_dataset.json        # 问题-答案标注数据
└── README.md                  # 数据目录总体说明
flowchart TD
    A[1. 数据收集] --> B[2. 数据清洗]
    B --> C[3. 数据标准化]
    C --> D[4. 数据增强]
    D --> E[5. 样本划分]
    E --> F[6. 质量检查]
    F --> G[高质量预处理数据]
    G --> H[知识库构建与向量化]
{
    "id": "KB_0001",
    "category": "课程教材",
    "title": "制造智能导论-第一章",
    "content": "制造智能是智能制造的核心技术领域...",
    "source": "《制造智能》教材",
    "timestamp": "2026-09-01",
    "annotation": {}
}
pie title 数据集划分
    "训练集" : 70
    "验证集" : 15
    "测试集" : 15
flowchart TD
    A[调度模块] --> B[数据读取模块]
    A --> C[数据清洗模块]
    A --> D[数据标准化模块]
    A --> E[数据增强模块]
    A --> F[样本划分模块]
    A --> G[统计评估模块]

    B --> H[JSON / TXT / CSV]
    C --> I[去重/去噪/纠错/长度过滤]
    D --> J[JSON+UTF-8格式统一]
    E --> K[同义词替换/句式改写]
    F --> L[训练/验证/测试集]
    G --> M[数据统计报告]
flowchart TD
    A[加载文档] --> B[文档分块]
    B --> C[向量化嵌入]
    C --> D[存储至向量数据库]

    E[用户输入问题] --> F[问题向量化]
    F --> G[相似度检索]
    D --> G
    G --> H[召回相关知识片段]
    H --> I[构建提示词]
    I --> J[大模型生成回答]
    J --> K[输出答案+引用来源]
# RAG系统核心实现（示例）
from langchain.document_loaders import DirectoryLoader
from langchain.text_splitter import RecursiveCharacterTextSplitter
from langchain.embeddings import HuggingFaceEmbeddings
from langchain.vectorstores import Chroma
from langchain.chains import RetrievalQA

# 1. 加载文档
loader = DirectoryLoader("data/knowledge_base/", glob="**/*.txt")
documents = loader.load()

# 2. 文档分块
text_splitter = RecursiveCharacterTextSplitter(
    chunk_size=500,
    chunk_overlap=50
)
texts = text_splitter.split_documents(documents)

# 3. 向量化与存储
embeddings = HuggingFaceEmbeddings(
    model_name="sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2"
)
vectorstore = Chroma.from_documents(texts, embeddings)

# 4. 检索问答链
qa_chain = RetrievalQA.from_chain_type(
    llm=llm,
    retriever=vectorstore.as_retriever(search_kwargs={"k": 5}),
    return_source_documents=True
)

# 5. 生成回答
result = qa_chain("什么是智能制造？")
print(result["result"])
print(result["source_documents"])
prompt/
├── 01_选题调研/
│   ├── PROMPT001_RAG原理讲解.md
│   └── PROMPT002_技术路线讨论.md
├── 02_工具学习/
│   ├── PROMPT003_LangChain入门.md
│   └── PROMPT004_向量数据库选型.md
├── 03_代码开发/
│   ├── PROMPT005_预处理程序生成.md
│   └── PROMPT006_检索模块调试.md
├── 04_文档撰写/
│   └── PROMPT007_报告大纲生成.md
├── 05_问题排查/
│   └── PROMPT008_编码错误修复.md
├── 索引文件.json
└── 学习笔记.md
{
    "id": "PROMPT001",
    "timestamp": "2026-09-01 10:30",
    "topic": "RAG原理讲解",
    "phase": "选题调研",
    "tool": "豆包大模型",
    "prompt": "请详细解释RAG技术的原理和优势...",
    "response_summary": "RAG通过检索外部知识库...",
    "adoption": "已采纳",
    "notes": "明确了RAG的技术路线"
}
    D --> E[大语言模型]
    A --> E
    E --> F[生成回答 + 引用来源]
